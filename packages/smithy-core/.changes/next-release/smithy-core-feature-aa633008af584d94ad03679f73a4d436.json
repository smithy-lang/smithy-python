{
  "type": "feature",
  "description": "Updated standard retry behavior with error-specific backoff, revised quota costs, bounded server-provided retry delays, support for service-specific defaults, and long-polling backoff when retry quota is exhausted."
}