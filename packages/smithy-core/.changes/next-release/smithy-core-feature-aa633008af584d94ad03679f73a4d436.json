{
  "type": "feature",
  "description": "Updated the standard retry behavior with separate throttling backoff, new retry quota costs, server-provided retry-after delays, per-service defaults, and long-polling backoff."
}