# Code generated by smithy-python-codegen DO NOT EDIT.

import base64
from datetime import datetime, timezone
from typing import Any
from urllib.parse import parse_qsl

from pytest import fail, mark, raises
from smithy_cbor import loads as _cbor_loads
from smithy_core.aio.retries import SimpleRetryStrategy
from smithy_core.aio.types import AsyncBytesReader
from smithy_core.aio.utils import async_list
from smithy_http import tuples_to_fields
from smithy_http.aio import HTTPResponse as _smithy_http_aio_HTTPResponse
from smithy_http.aio.interfaces import (
    HTTPRequest,
    HTTPResponse as _smithy_http_aio_interfaces_HTTPResponse,
)
from smithy_http.interfaces import HTTPClientConfiguration, HTTPRequestConfiguration
from smithy_test import deep_equal

from rpcv2cbor.client import RpcV2Protocol
from rpcv2cbor.config import Config
from rpcv2cbor.models import (
    ClientOptionalDefaults,
    Defaults,
    EmptyInputOutputInput,
    EmptyInputOutputOutput,
    Float16Input,
    Float16Output,
    FractionalSecondsInput,
    FractionalSecondsOutput,
    GreetingStruct,
    GreetingWithErrorsInput,
    NoInputOutputInput,
    NoInputOutputOutput,
    OperationWithDefaultsInput,
    OperationWithDefaultsOutput,
    OptionalInputOutputInput,
    OptionalInputOutputOutput,
    RecursiveShapesInput,
    RecursiveShapesInputOutputNested1,
    RecursiveShapesInputOutputNested2,
    RecursiveShapesOutput,
    RpcV2CborDenseMapsInput,
    RpcV2CborDenseMapsOutput,
    RpcV2CborListsInput,
    RpcV2CborListsOutput,
    RpcV2CborNestedUnionStringValue,
    RpcV2CborSparseMapsInput,
    RpcV2CborSparseMapsOutput,
    RpcV2CborUnionStringValue,
    RpcV2CborUnionUnionValue,
    RpcV2CborUnionsInput,
    RpcV2CborUnionsOutput,
    ServiceError,
    SimpleScalarPropertiesInput,
    SimpleScalarPropertiesOutput,
    SparseNullsOperationInput,
    SparseNullsOperationOutput,
    StructureListMember,
)


async def test_empty_input_request_empty_input_output() -> None:
    """When Input structure is empty we write CBOR equivalent of {}"""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
        )
    )

    input_ = EmptyInputOutputInput()

    try:
        await client.empty_input_output(input_)
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert (
            actual.destination.path
            == "/service/RpcV2Protocol/operation/EmptyInputOutput"
        )
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("accept", "application/cbor"),
            ("content-type", "application/cbor"),
            ("smithy-protocol", "rpc-v2-cbor"),
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set(["x-amz-target"])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = ["content-length"]
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = base64.b64decode(b"v/8=")
        actual_body: Any = (
            _cbor_loads(actual_body_content) if actual_body_content else {}
        )
        expected_body: Any = (
            _cbor_loads(expected_body_content) if expected_body_content else {}
        )
        assert deep_equal(actual_body, expected_body)

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


async def test_empty_output_response_empty_input_output() -> None:
    """When output structure is empty we write CBOR equivalent of {}"""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[
                    ("Content-Type", "application/cbor"),
                    ("smithy-protocol", "rpc-v2-cbor"),
                ],
                body=base64.b64decode(b"v/8="),
            ),
        )
    )

    input_ = EmptyInputOutputInput()

    try:
        actual = await client.empty_input_output(input_)
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = EmptyInputOutputOutput()

        assert deep_equal(actual, expected)


async def test_empty_output_no_body_response_empty_input_output() -> None:
    """When output structure is empty the client should accept an empty body"""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[
                    ("Content-Type", "application/cbor"),
                    ("smithy-protocol", "rpc-v2-cbor"),
                ],
                body=base64.b64decode(b""),
            ),
        )
    )

    input_ = EmptyInputOutputInput()

    try:
        actual = await client.empty_input_output(input_)
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = EmptyInputOutputOutput()

        assert deep_equal(actual, expected)


async def test_rpc_v2_cbor_float16_inf_response_float16() -> None:
    """Ensures that clients can correctly parse float16 +Inf."""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[
                    ("Content-Type", "application/cbor"),
                    ("smithy-protocol", "rpc-v2-cbor"),
                ],
                body=base64.b64decode(b"oWV2YWx1Zfl8AA=="),
            ),
        )
    )

    input_ = Float16Input()

    try:
        actual = await client.float16(input_)
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = Float16Output(value=float("inf"))

        assert deep_equal(actual, expected)


async def test_rpc_v2_cbor_float16_neg_inf_response_float16() -> None:
    """Ensures that clients can correctly parse float16 -Inf."""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[
                    ("Content-Type", "application/cbor"),
                    ("smithy-protocol", "rpc-v2-cbor"),
                ],
                body=base64.b64decode(b"oWV2YWx1Zfn8AA=="),
            ),
        )
    )

    input_ = Float16Input()

    try:
        actual = await client.float16(input_)
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = Float16Output(value=float("-inf"))

        assert deep_equal(actual, expected)


async def test_rpc_v2_cbor_float16_lsb_na_n_response_float16() -> None:
    """Ensures that clients can correctly parse float16 NaN with high LSB."""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[
                    ("Content-Type", "application/cbor"),
                    ("smithy-protocol", "rpc-v2-cbor"),
                ],
                body=base64.b64decode(b"oWV2YWx1Zfl8AQ=="),
            ),
        )
    )

    input_ = Float16Input()

    try:
        actual = await client.float16(input_)
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = Float16Output(value=float("nan"))

        assert deep_equal(actual, expected)


async def test_rpc_v2_cbor_float16_msb_na_n_response_float16() -> None:
    """Ensures that clients can correctly parse float16 NaN with high MSB."""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[
                    ("Content-Type", "application/cbor"),
                    ("smithy-protocol", "rpc-v2-cbor"),
                ],
                body=base64.b64decode(b"oWV2YWx1Zfl+AA=="),
            ),
        )
    )

    input_ = Float16Input()

    try:
        actual = await client.float16(input_)
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = Float16Output(value=float("nan"))

        assert deep_equal(actual, expected)


async def test_rpc_v2_cbor_float16_subnormal_response_float16() -> None:
    """Ensures that clients can correctly parse a subnormal float16."""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[
                    ("Content-Type", "application/cbor"),
                    ("smithy-protocol", "rpc-v2-cbor"),
                ],
                body=base64.b64decode(b"oWV2YWx1ZfkAUA=="),
            ),
        )
    )

    input_ = Float16Input()

    try:
        actual = await client.float16(input_)
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = Float16Output(value=float(4.76837158203125e-6))

        assert deep_equal(actual, expected)


async def test_rpc_v2_cbor_date_time_with_fractional_seconds_response_fractional_seconds() -> (
    None
):
    """
    Ensures that clients can correctly parse timestamps with fractional
    seconds
    """
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[
                    ("Content-Type", "application/cbor"),
                    ("smithy-protocol", "rpc-v2-cbor"),
                ],
                body=base64.b64decode(b"v2hkYXRldGltZcH7Qcw32zgPvnf/"),
            ),
        )
    )

    input_ = FractionalSecondsInput()

    try:
        actual = await client.fractional_seconds(input_)
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = FractionalSecondsOutput(
            datetime_=datetime(2000, 1, 2, 20, 34, 56, 123000, timezone.utc)
        )

        assert deep_equal(actual, expected)


async def test_rpc_v2_cbor_invalid_greeting_error_error_greeting_with_errors() -> None:
    """Parses simple RpcV2 Cbor errors"""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=400,
                headers=[
                    ("Content-Type", "application/cbor"),
                    ("smithy-protocol", "rpc-v2-cbor"),
                ],
                body=base64.b64decode(
                    b"v2ZfX3R5cGV4LnNtaXRoeS5wcm90b2NvbHRlc3RzLnJwY3YyQ2JvciNJbnZhbGlkR3JlZXRpbmdnTWVzc2FnZWJIaf8="
                ),
            ),
        )
    )

    input_ = GreetingWithErrorsInput()

    try:
        await client.greeting_with_errors(input_)
        fail("Expected 'InvalidGreeting' exception to be thrown!")
    except Exception as err:
        if type(err).__name__ != "InvalidGreeting":
            fail(
                f"Expected 'InvalidGreeting' exception to be thrown, but received {type(err).__name__}: {err}"
            )


async def test_rpc_v2_cbor_complex_error_error_greeting_with_errors() -> None:
    """Parses a complex error with no message member"""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=400,
                headers=[
                    ("Content-Type", "application/cbor"),
                    ("smithy-protocol", "rpc-v2-cbor"),
                ],
                body=base64.b64decode(
                    b"v2ZfX3R5cGV4K3NtaXRoeS5wcm90b2NvbHRlc3RzLnJwY3YyQ2JvciNDb21wbGV4RXJyb3JoVG9wTGV2ZWxpVG9wIGxldmVsZk5lc3RlZL9jRm9vY2Jhcv//"
                ),
            ),
        )
    )

    input_ = GreetingWithErrorsInput()

    try:
        await client.greeting_with_errors(input_)
        fail("Expected 'ComplexError' exception to be thrown!")
    except Exception as err:
        if type(err).__name__ != "ComplexError":
            fail(
                f"Expected 'ComplexError' exception to be thrown, but received {type(err).__name__}: {err}"
            )


async def test_rpc_v2_cbor_empty_complex_error_error_greeting_with_errors() -> None:
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=400,
                headers=[
                    ("Content-Type", "application/cbor"),
                    ("smithy-protocol", "rpc-v2-cbor"),
                ],
                body=base64.b64decode(
                    b"v2ZfX3R5cGV4K3NtaXRoeS5wcm90b2NvbHRlc3RzLnJwY3YyQ2JvciNDb21wbGV4RXJyb3L/"
                ),
            ),
        )
    )

    input_ = GreetingWithErrorsInput()

    try:
        await client.greeting_with_errors(input_)
        fail("Expected 'ComplexError' exception to be thrown!")
    except Exception as err:
        if type(err).__name__ != "ComplexError":
            fail(
                f"Expected 'ComplexError' exception to be thrown, but received {type(err).__name__}: {err}"
            )


async def test_no_input_request_no_input_output() -> None:
    """Body is empty and no Content-Type header if no input"""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
        )
    )

    input_ = NoInputOutputInput()

    try:
        await client.no_input_output(input_)
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert (
            actual.destination.path == "/service/RpcV2Protocol/operation/NoInputOutput"
        )
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("accept", "application/cbor"),
            ("smithy-protocol", "rpc-v2-cbor"),
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set(["content-type", "x-amz-target"])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = []
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = b""
        assert actual_body_content == expected_body_content

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


async def test_no_output_response_no_input_output() -> None:
    """A `Content-Type` header should not be set if the response body is empty."""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[("smithy-protocol", "rpc-v2-cbor")],
                body=base64.b64decode(b""),
            ),
        )
    )

    input_ = NoInputOutputInput()

    try:
        actual = await client.no_input_output(input_)
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = NoInputOutputOutput()

        assert deep_equal(actual, expected)


async def test_no_output_client_allows_empty_cbor_response_no_input_output() -> None:
    """Clients should accept a CBOR empty struct if there is no output."""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[
                    ("Content-Type", "application/cbor"),
                    ("smithy-protocol", "rpc-v2-cbor"),
                ],
                body=base64.b64decode(b"v/8="),
            ),
        )
    )

    input_ = NoInputOutputInput()

    try:
        actual = await client.no_input_output(input_)
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = NoInputOutputOutput()

        assert deep_equal(actual, expected)


async def test_no_output_client_allows_empty_body_response_no_input_output() -> None:
    """
    Clients should accept an empty body if there is no output and should not
    raise an error if the `Content-Type` header is set.
    """
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[
                    ("Content-Type", "application/cbor"),
                    ("smithy-protocol", "rpc-v2-cbor"),
                ],
                body=base64.b64decode(b""),
            ),
        )
    )

    input_ = NoInputOutputInput()

    try:
        actual = await client.no_input_output(input_)
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = NoInputOutputOutput()

        assert deep_equal(actual, expected)


async def test_rpc_v2_cbor_client_populates_default_values_in_input_request_operation_with_defaults() -> (
    None
):
    """Client populates default values in input."""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
        )
    )

    input_ = OperationWithDefaultsInput(defaults=Defaults())

    try:
        await client.operation_with_defaults(input_)
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert (
            actual.destination.path
            == "/service/RpcV2Protocol/operation/OperationWithDefaults"
        )
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("accept", "application/cbor"),
            ("content-type", "application/cbor"),
            ("smithy-protocol", "rpc-v2-cbor"),
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = ["content-length"]
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = base64.b64decode(
            b""
        )
        actual_body: Any = (
            _cbor_loads(actual_body_content) if actual_body_content else {}
        )
        expected_body: Any = (
            _cbor_loads(expected_body_content) if expected_body_content else {}
        )
        assert deep_equal(actual_body, expected_body)

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


async def test_rpc_v2_cbor_client_skips_top_level_default_values_in_input_request_operation_with_defaults() -> (
    None
):
    """Client skips top level default values in input."""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
        )
    )

    input_ = OperationWithDefaultsInput()

    try:
        await client.operation_with_defaults(input_)
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert (
            actual.destination.path
            == "/service/RpcV2Protocol/operation/OperationWithDefaults"
        )
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("accept", "application/cbor"),
            ("content-type", "application/cbor"),
            ("smithy-protocol", "rpc-v2-cbor"),
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = ["content-length"]
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = base64.b64decode(b"v/8=")
        actual_body: Any = (
            _cbor_loads(actual_body_content) if actual_body_content else {}
        )
        expected_body: Any = (
            _cbor_loads(expected_body_content) if expected_body_content else {}
        )
        assert deep_equal(actual_body, expected_body)

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


async def test_rpc_v2_cbor_client_uses_explicitly_provided_member_values_over_defaults_request_operation_with_defaults() -> (
    None
):
    """Client uses explicitly provided member values over defaults"""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
        )
    )

    input_ = OperationWithDefaultsInput(
        defaults=Defaults(
            default_string="bye",
            default_boolean=True,
            default_list=["a"],
            default_timestamp=datetime(1970, 1, 1, 0, 0, 1, 0, timezone.utc),
            default_blob=b"hi",
            default_byte=2,
            default_short=2,
            default_integer=20,
            default_long=200,
            default_float=float(2.0),
            default_double=float(2.0),
            default_map={"name": "Jack"},
            default_enum="BAR",
            default_int_enum=2,
            empty_string="foo",
            false_boolean=True,
            empty_blob=b"hi",
            zero_byte=1,
            zero_short=1,
            zero_integer=1,
            zero_long=1,
            zero_float=float(1.0),
            zero_double=float(1.0),
        )
    )

    try:
        await client.operation_with_defaults(input_)
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert (
            actual.destination.path
            == "/service/RpcV2Protocol/operation/OperationWithDefaults"
        )
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("accept", "application/cbor"),
            ("content-type", "application/cbor"),
            ("smithy-protocol", "rpc-v2-cbor"),
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = ["content-length"]
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = base64.b64decode(
            b""
        )
        actual_body: Any = (
            _cbor_loads(actual_body_content) if actual_body_content else {}
        )
        expected_body: Any = (
            _cbor_loads(expected_body_content) if expected_body_content else {}
        )
        assert deep_equal(actual_body, expected_body)

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


@mark.xfail()
async def test_rpc_v2_cbor_client_uses_explicitly_provided_values_in_top_level_request_operation_with_defaults() -> (
    None
):
    """
    Any time a value is provided for a member in the top level of input, it
    is used, regardless of if its the default.
    """
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
        )
    )

    input_ = OperationWithDefaultsInput(
        top_level_default="hi", other_top_level_default=0
    )

    try:
        await client.operation_with_defaults(input_)
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert (
            actual.destination.path
            == "/service/RpcV2Protocol/operation/OperationWithDefaults"
        )
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("accept", "application/cbor"),
            ("content-type", "application/cbor"),
            ("smithy-protocol", "rpc-v2-cbor"),
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = ["content-length"]
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = base64.b64decode(
            b"v290b3BMZXZlbERlZmF1bHRiaGl0b3RoZXJUb3BMZXZlbERlZmF1bHQA/w=="
        )
        actual_body: Any = (
            _cbor_loads(actual_body_content) if actual_body_content else {}
        )
        expected_body: Any = (
            _cbor_loads(expected_body_content) if expected_body_content else {}
        )
        assert deep_equal(actual_body, expected_body)

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


@mark.xfail()
async def test_rpc_v2_cbor_client_ignores_non_top_level_defaults_on_members_with_client_optional_request_operation_with_defaults() -> (
    None
):
    """
    Typically, non top-level members would have defaults filled in, but if
    they have the clientOptional trait, the defaults should be ignored.
    """
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
        )
    )

    input_ = OperationWithDefaultsInput(
        client_optional_defaults=ClientOptionalDefaults()
    )

    try:
        await client.operation_with_defaults(input_)
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert (
            actual.destination.path
            == "/service/RpcV2Protocol/operation/OperationWithDefaults"
        )
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("accept", "application/cbor"),
            ("content-type", "application/cbor"),
            ("smithy-protocol", "rpc-v2-cbor"),
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = ["content-length"]
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = base64.b64decode(
            b"v3ZjbGllbnRPcHRpb25hbERlZmF1bHRzoP8="
        )
        actual_body: Any = (
            _cbor_loads(actual_body_content) if actual_body_content else {}
        )
        expected_body: Any = (
            _cbor_loads(expected_body_content) if expected_body_content else {}
        )
        assert deep_equal(actual_body, expected_body)

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


async def test_rpc_v2_cbor_client_populates_defaults_values_when_missing_in_response_response_operation_with_defaults() -> (
    None
):
    """Client populates default values when missing in response."""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[
                    ("Content-Type", "application/cbor"),
                    ("smithy-protocol", "rpc-v2-cbor"),
                ],
                body=base64.b64decode(b"v/8="),
            ),
        )
    )

    input_ = OperationWithDefaultsInput()

    try:
        actual = await client.operation_with_defaults(input_)
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = OperationWithDefaultsOutput(
            default_string="hi",
            default_boolean=True,
            default_list=[],
            default_timestamp=datetime(1970, 1, 1, 0, 0, 0, 0, timezone.utc),
            default_blob=b"abc",
            default_byte=1,
            default_short=1,
            default_integer=10,
            default_long=100,
            default_float=float(1.0),
            default_double=float(1.0),
            default_map={},
            default_enum="FOO",
            default_int_enum=1,
            empty_string="",
            false_boolean=False,
            empty_blob=b"",
            zero_byte=0,
            zero_short=0,
            zero_integer=0,
            zero_long=0,
            zero_float=float(0.0),
            zero_double=float(0.0),
        )

        assert deep_equal(actual, expected)


async def test_rpc_v2_cbor_client_ignores_default_values_if_member_values_are_present_in_response_response_operation_with_defaults() -> (
    None
):
    """
    Client ignores default values if member values are present in the
    response.
    """
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[
                    ("Content-Type", "application/cbor"),
                    ("smithy-protocol", "rpc-v2-cbor"),
                ],
                body=base64.b64decode(
                    b""
                ),
            ),
        )
    )

    input_ = OperationWithDefaultsInput()

    try:
        actual = await client.operation_with_defaults(input_)
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = OperationWithDefaultsOutput(
            default_string="bye",
            default_boolean=False,
            default_list=["a"],
            default_timestamp=datetime(1970, 1, 1, 0, 0, 2, 0, timezone.utc),
            default_blob=b"hi",
            default_byte=2,
            default_short=2,
            default_integer=20,
            default_long=200,
            default_float=float(2.0),
            default_double=float(2.0),
            default_map={"name": "Jack"},
            default_enum="BAR",
            default_int_enum=2,
            empty_string="foo",
            false_boolean=True,
            empty_blob=b"hi",
            zero_byte=1,
            zero_short=1,
            zero_integer=1,
            zero_long=1,
            zero_float=float(1.0),
            zero_double=float(1.0),
        )

        assert deep_equal(actual, expected)


async def test_optional_input_request_optional_input_output() -> None:
    """When input is empty we write CBOR equivalent of {}"""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
        )
    )

    input_ = OptionalInputOutputInput()

    try:
        await client.optional_input_output(input_)
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert (
            actual.destination.path
            == "/service/RpcV2Protocol/operation/OptionalInputOutput"
        )
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("accept", "application/cbor"),
            ("content-type", "application/cbor"),
            ("smithy-protocol", "rpc-v2-cbor"),
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set(["x-amz-target"])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = []
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = base64.b64decode(b"v/8=")
        actual_body: Any = (
            _cbor_loads(actual_body_content) if actual_body_content else {}
        )
        expected_body: Any = (
            _cbor_loads(expected_body_content) if expected_body_content else {}
        )
        assert deep_equal(actual_body, expected_body)

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


async def test_optional_output_response_optional_input_output() -> None:
    """When output is empty we write CBOR equivalent of {}"""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[
                    ("Content-Type", "application/cbor"),
                    ("smithy-protocol", "rpc-v2-cbor"),
                ],
                body=base64.b64decode(b"v/8="),
            ),
        )
    )

    input_ = OptionalInputOutputInput()

    try:
        actual = await client.optional_input_output(input_)
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = OptionalInputOutputOutput()

        assert deep_equal(actual, expected)


async def test_rpc_v2_cbor_recursive_shapes_request_recursive_shapes() -> None:
    """Serializes recursive structures"""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
        )
    )

    input_ = RecursiveShapesInput(
        nested=RecursiveShapesInputOutputNested1(
            foo="Foo1",
            nested=RecursiveShapesInputOutputNested2(
                bar="Bar1",
                recursive_member=RecursiveShapesInputOutputNested1(
                    foo="Foo2", nested=RecursiveShapesInputOutputNested2(bar="Bar2")
                ),
            ),
        )
    )

    try:
        await client.recursive_shapes(input_)
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert (
            actual.destination.path
            == "/service/RpcV2Protocol/operation/RecursiveShapes"
        )
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("accept", "application/cbor"),
            ("content-type", "application/cbor"),
            ("smithy-protocol", "rpc-v2-cbor"),
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = ["content-length"]
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = base64.b64decode(
            b"v2ZuZXN0ZWS/Y2Zvb2RGb28xZm5lc3RlZL9jYmFyZEJhcjFvcmVjdXJzaXZlTWVtYmVyv2Nmb29kRm9vMmZuZXN0ZWS/Y2JhcmRCYXIy//////8="
        )
        actual_body: Any = (
            _cbor_loads(actual_body_content) if actual_body_content else {}
        )
        expected_body: Any = (
            _cbor_loads(expected_body_content) if expected_body_content else {}
        )
        assert deep_equal(actual_body, expected_body)

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


async def test_rpc_v2_cbor_recursive_shapes_response_recursive_shapes() -> None:
    """Serializes recursive structures"""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[
                    ("Content-Type", "application/cbor"),
                    ("smithy-protocol", "rpc-v2-cbor"),
                ],
                body=base64.b64decode(
                    b"v2ZuZXN0ZWS/Y2Zvb2RGb28xZm5lc3RlZL9jYmFyZEJhcjFvcmVjdXJzaXZlTWVtYmVyv2Nmb29kRm9vMmZuZXN0ZWS/Y2JhcmRCYXIy//////8="
                ),
            ),
        )
    )

    input_ = RecursiveShapesInput()

    try:
        actual = await client.recursive_shapes(input_)
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = RecursiveShapesOutput(
            nested=RecursiveShapesInputOutputNested1(
                foo="Foo1",
                nested=RecursiveShapesInputOutputNested2(
                    bar="Bar1",
                    recursive_member=RecursiveShapesInputOutputNested1(
                        foo="Foo2", nested=RecursiveShapesInputOutputNested2(bar="Bar2")
                    ),
                ),
            )
        )

        assert deep_equal(actual, expected)


async def test_rpc_v2_cbor_recursive_shapes_using_definite_length_response_recursive_shapes() -> (
    None
):
    """
    Deserializes recursive structures encoded using a map with definite
    length
    """
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[
                    ("Content-Type", "application/cbor"),
                    ("smithy-protocol", "rpc-v2-cbor"),
                ],
                body=base64.b64decode(
                    b"oWZuZXN0ZWSiY2Zvb2RGb28xZm5lc3RlZKJjYmFyZEJhcjFvcmVjdXJzaXZlTWVtYmVyomNmb29kRm9vMmZuZXN0ZWShY2JhcmRCYXIy"
                ),
            ),
        )
    )

    input_ = RecursiveShapesInput()

    try:
        actual = await client.recursive_shapes(input_)
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = RecursiveShapesOutput(
            nested=RecursiveShapesInputOutputNested1(
                foo="Foo1",
                nested=RecursiveShapesInputOutputNested2(
                    bar="Bar1",
                    recursive_member=RecursiveShapesInputOutputNested1(
                        foo="Foo2", nested=RecursiveShapesInputOutputNested2(bar="Bar2")
                    ),
                ),
            )
        )

        assert deep_equal(actual, expected)


async def test_rpc_v2_cbor_maps_request_rpc_v2_cbor_dense_maps() -> None:
    """Serializes maps"""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
        )
    )

    input_ = RpcV2CborDenseMapsInput(
        dense_struct_map={
            "foo": GreetingStruct(hi="there"),
            "baz": GreetingStruct(hi="bye"),
        }
    )

    try:
        await client.rpc_v2_cbor_dense_maps(input_)
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert (
            actual.destination.path
            == "/service/RpcV2Protocol/operation/RpcV2CborDenseMaps"
        )
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("accept", "application/cbor"),
            ("content-type", "application/cbor"),
            ("smithy-protocol", "rpc-v2-cbor"),
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = ["content-length"]
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = base64.b64decode(
            b"oW5kZW5zZVN0cnVjdE1hcKJjZm9voWJoaWV0aGVyZWNiYXqhYmhpY2J5ZQ=="
        )
        actual_body: Any = (
            _cbor_loads(actual_body_content) if actual_body_content else {}
        )
        expected_body: Any = (
            _cbor_loads(expected_body_content) if expected_body_content else {}
        )
        assert deep_equal(actual_body, expected_body)

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


async def test_rpc_v2_cbor_serializes_zero_values_in_maps_request_rpc_v2_cbor_dense_maps() -> (
    None
):
    """Ensure that 0 and false are sent over the wire in all maps and lists"""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
        )
    )

    input_ = RpcV2CborDenseMapsInput(
        dense_number_map={"x": 0}, dense_boolean_map={"x": False}
    )

    try:
        await client.rpc_v2_cbor_dense_maps(input_)
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert (
            actual.destination.path
            == "/service/RpcV2Protocol/operation/RpcV2CborDenseMaps"
        )
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("accept", "application/cbor"),
            ("content-type", "application/cbor"),
            ("smithy-protocol", "rpc-v2-cbor"),
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = ["content-length"]
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = base64.b64decode(
            b"om5kZW5zZU51bWJlck1hcKFheABvZGVuc2VCb29sZWFuTWFwoWF49A=="
        )
        actual_body: Any = (
            _cbor_loads(actual_body_content) if actual_body_content else {}
        )
        expected_body: Any = (
            _cbor_loads(expected_body_content) if expected_body_content else {}
        )
        assert deep_equal(actual_body, expected_body)

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


async def test_rpc_v2_cbor_serializes_dense_set_map_request_rpc_v2_cbor_dense_maps() -> (
    None
):
    """A request that contains a dense map of sets."""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
        )
    )

    input_ = RpcV2CborDenseMapsInput(dense_set_map={"x": [], "y": ["a", "b"]})

    try:
        await client.rpc_v2_cbor_dense_maps(input_)
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert (
            actual.destination.path
            == "/service/RpcV2Protocol/operation/RpcV2CborDenseMaps"
        )
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("accept", "application/cbor"),
            ("content-type", "application/cbor"),
            ("smithy-protocol", "rpc-v2-cbor"),
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = ["content-length"]
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = base64.b64decode(b"oWtkZW5zZVNldE1hcKJheIBheYJhYWFi")
        actual_body: Any = (
            _cbor_loads(actual_body_content) if actual_body_content else {}
        )
        expected_body: Any = (
            _cbor_loads(expected_body_content) if expected_body_content else {}
        )
        assert deep_equal(actual_body, expected_body)

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


async def test_rpc_v2_cbor_maps_response_rpc_v2_cbor_dense_maps() -> None:
    """Deserializes maps"""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[
                    ("Content-Type", "application/cbor"),
                    ("smithy-protocol", "rpc-v2-cbor"),
                ],
                body=base64.b64decode(
                    b"oW5kZW5zZVN0cnVjdE1hcKJjZm9voWJoaWV0aGVyZWNiYXqhYmhpY2J5ZQ=="
                ),
            ),
        )
    )

    input_ = RpcV2CborDenseMapsInput()

    try:
        actual = await client.rpc_v2_cbor_dense_maps(input_)
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = RpcV2CborDenseMapsOutput(
            dense_struct_map={
                "foo": GreetingStruct(hi="there"),
                "baz": GreetingStruct(hi="bye"),
            }
        )

        assert deep_equal(actual, expected)


async def test_rpc_v2_cbor_deserializes_zero_values_in_maps_response_rpc_v2_cbor_dense_maps() -> (
    None
):
    """Ensure that 0 and false are sent over the wire in all maps and lists"""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[
                    ("Content-Type", "application/cbor"),
                    ("smithy-protocol", "rpc-v2-cbor"),
                ],
                body=base64.b64decode(
                    b"om5kZW5zZU51bWJlck1hcKFheABvZGVuc2VCb29sZWFuTWFwoWF49A=="
                ),
            ),
        )
    )

    input_ = RpcV2CborDenseMapsInput()

    try:
        actual = await client.rpc_v2_cbor_dense_maps(input_)
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = RpcV2CborDenseMapsOutput(
            dense_number_map={"x": 0}, dense_boolean_map={"x": False}
        )

        assert deep_equal(actual, expected)


async def test_rpc_v2_cbor_deserializes_dense_set_map_response_rpc_v2_cbor_dense_maps() -> (
    None
):
    """A response that contains a dense map of sets"""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[
                    ("Content-Type", "application/cbor"),
                    ("smithy-protocol", "rpc-v2-cbor"),
                ],
                body=base64.b64decode(b"oWtkZW5zZVNldE1hcKJheIBheYJhYWFi"),
            ),
        )
    )

    input_ = RpcV2CborDenseMapsInput()

    try:
        actual = await client.rpc_v2_cbor_dense_maps(input_)
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = RpcV2CborDenseMapsOutput(dense_set_map={"x": [], "y": ["a", "b"]})

        assert deep_equal(actual, expected)


async def test_rpc_v2_cbor_lists_request_rpc_v2_cbor_lists() -> None:
    """Serializes RpcV2 Cbor lists"""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
        )
    )

    input_ = RpcV2CborListsInput(
        string_list=["foo", "bar"],
        string_set=["foo", "bar"],
        integer_list=[1, 2],
        boolean_list=[True, False],
        timestamp_list=[
            datetime(2014, 4, 29, 18, 30, 38, 0, timezone.utc),
            datetime(2014, 4, 29, 18, 30, 38, 0, timezone.utc),
        ],
        enum_list=["Foo", "0"],
        int_enum_list=[1, 2],
        nested_string_list=[["foo", "bar"], ["baz", "qux"]],
        structure_list=[
            StructureListMember(a="1", b="2"),
            StructureListMember(a="3", b="4"),
        ],
        blob_list=[b"foo", b"bar"],
    )

    try:
        await client.rpc_v2_cbor_lists(input_)
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert (
            actual.destination.path == "/service/RpcV2Protocol/operation/RpcV2CborLists"
        )
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("accept", "application/cbor"),
            ("content-type", "application/cbor"),
            ("smithy-protocol", "rpc-v2-cbor"),
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = ["content-length"]
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = base64.b64decode(
            b""
        )
        actual_body: Any = (
            _cbor_loads(actual_body_content) if actual_body_content else {}
        )
        expected_body: Any = (
            _cbor_loads(expected_body_content) if expected_body_content else {}
        )
        assert deep_equal(actual_body, expected_body)

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


async def test_rpc_v2_cbor_lists_empty_request_rpc_v2_cbor_lists() -> None:
    """Serializes empty JSON lists"""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
        )
    )

    input_ = RpcV2CborListsInput(string_list=[])

    try:
        await client.rpc_v2_cbor_lists(input_)
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert (
            actual.destination.path == "/service/RpcV2Protocol/operation/RpcV2CborLists"
        )
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("accept", "application/cbor"),
            ("content-type", "application/cbor"),
            ("smithy-protocol", "rpc-v2-cbor"),
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = ["content-length"]
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = base64.b64decode(b"v2pzdHJpbmdMaXN0n///")
        actual_body: Any = (
            _cbor_loads(actual_body_content) if actual_body_content else {}
        )
        expected_body: Any = (
            _cbor_loads(expected_body_content) if expected_body_content else {}
        )
        assert deep_equal(actual_body, expected_body)

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


async def test_rpc_v2_cbor_lists_empty_using_definite_length_request_rpc_v2_cbor_lists() -> (
    None
):
    """Serializes empty JSON definite length lists"""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
        )
    )

    input_ = RpcV2CborListsInput(string_list=[])

    try:
        await client.rpc_v2_cbor_lists(input_)
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert (
            actual.destination.path == "/service/RpcV2Protocol/operation/RpcV2CborLists"
        )
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("accept", "application/cbor"),
            ("content-type", "application/cbor"),
            ("smithy-protocol", "rpc-v2-cbor"),
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = ["content-length"]
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = base64.b64decode(b"oWpzdHJpbmdMaXN0gA==")
        actual_body: Any = (
            _cbor_loads(actual_body_content) if actual_body_content else {}
        )
        expected_body: Any = (
            _cbor_loads(expected_body_content) if expected_body_content else {}
        )
        assert deep_equal(actual_body, expected_body)

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


async def test_rpc_v2_cbor_lists_response_rpc_v2_cbor_lists() -> None:
    """Serializes RpcV2 Cbor lists"""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[
                    ("Content-Type", "application/cbor"),
                    ("smithy-protocol", "rpc-v2-cbor"),
                ],
                body=base64.b64decode(
                    b""
                ),
            ),
        )
    )

    input_ = RpcV2CborListsInput()

    try:
        actual = await client.rpc_v2_cbor_lists(input_)
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = RpcV2CborListsOutput(
            string_list=["foo", "bar"],
            string_set=["foo", "bar"],
            integer_list=[1, 2],
            boolean_list=[True, False],
            timestamp_list=[
                datetime(2014, 4, 29, 18, 30, 38, 0, timezone.utc),
                datetime(2014, 4, 29, 18, 30, 38, 0, timezone.utc),
            ],
            enum_list=["Foo", "0"],
            int_enum_list=[1, 2],
            nested_string_list=[["foo", "bar"], ["baz", "qux"]],
            structure_list=[
                StructureListMember(a="1", b="2"),
                StructureListMember(a="3", b="4"),
            ],
            blob_list=[b"foo", b"bar"],
        )

        assert deep_equal(actual, expected)


async def test_rpc_v2_cbor_lists_empty_response_rpc_v2_cbor_lists() -> None:
    """Serializes empty RpcV2 Cbor lists"""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[
                    ("Content-Type", "application/cbor"),
                    ("smithy-protocol", "rpc-v2-cbor"),
                ],
                body=base64.b64decode(b"v2pzdHJpbmdMaXN0n///"),
            ),
        )
    )

    input_ = RpcV2CborListsInput()

    try:
        actual = await client.rpc_v2_cbor_lists(input_)
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = RpcV2CborListsOutput(string_list=[])

        assert deep_equal(actual, expected)


async def test_rpc_v2_cbor_indefinite_string_inside_indefinite_list_can_deserialize_response_rpc_v2_cbor_lists() -> (
    None
):
    """
    Can deserialize indefinite length text strings inside an indefinite
    length list
    """
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[
                    ("Content-Type", "application/cbor"),
                    ("smithy-protocol", "rpc-v2-cbor"),
                ],
                body=base64.b64decode(
                    b"v2pzdHJpbmdMaXN0n394HUFuIGV4YW1wbGUgaW5kZWZpbml0ZSBzdHJpbmcsdyB3aGljaCB3aWxsIGJlIGNodW5rZWQsbiBvbiBlYWNoIGNvbW1h/394NUFub3RoZXIgZXhhbXBsZSBpbmRlZmluaXRlIHN0cmluZyB3aXRoIG9ubHkgb25lIGNodW5r/3ZUaGlzIGlzIGEgcGxhaW4gc3RyaW5n//8="
                ),
            ),
        )
    )

    input_ = RpcV2CborListsInput()

    try:
        actual = await client.rpc_v2_cbor_lists(input_)
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = RpcV2CborListsOutput(
            string_list=[
                "An example indefinite string, which will be chunked, on each comma",
                "Another example indefinite string with only one chunk",
                "This is a plain string",
            ]
        )

        assert deep_equal(actual, expected)


async def test_rpc_v2_cbor_indefinite_string_inside_definite_list_can_deserialize_response_rpc_v2_cbor_lists() -> (
    None
):
    """
    Can deserialize indefinite length text strings inside a definite length
    list
    """
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[
                    ("Content-Type", "application/cbor"),
                    ("smithy-protocol", "rpc-v2-cbor"),
                ],
                body=base64.b64decode(
                    b"oWpzdHJpbmdMaXN0g394HUFuIGV4YW1wbGUgaW5kZWZpbml0ZSBzdHJpbmcsdyB3aGljaCB3aWxsIGJlIGNodW5rZWQsbiBvbiBlYWNoIGNvbW1h/394NUFub3RoZXIgZXhhbXBsZSBpbmRlZmluaXRlIHN0cmluZyB3aXRoIG9ubHkgb25lIGNodW5r/3ZUaGlzIGlzIGEgcGxhaW4gc3RyaW5n"
                ),
            ),
        )
    )

    input_ = RpcV2CborListsInput()

    try:
        actual = await client.rpc_v2_cbor_lists(input_)
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = RpcV2CborListsOutput(
            string_list=[
                "An example indefinite string, which will be chunked, on each comma",
                "Another example indefinite string with only one chunk",
                "This is a plain string",
            ]
        )

        assert deep_equal(actual, expected)


async def test_rpc_v2_cbor_sparse_maps_request_rpc_v2_cbor_sparse_maps() -> None:
    """Serializes sparse maps"""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
        )
    )

    input_ = RpcV2CborSparseMapsInput(
        sparse_struct_map={
            "foo": GreetingStruct(hi="there"),
            "baz": GreetingStruct(hi="bye"),
        }
    )

    try:
        await client.rpc_v2_cbor_sparse_maps(input_)
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert (
            actual.destination.path
            == "/service/RpcV2Protocol/operation/RpcV2CborSparseMaps"
        )
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("accept", "application/cbor"),
            ("content-type", "application/cbor"),
            ("smithy-protocol", "rpc-v2-cbor"),
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = ["content-length"]
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = base64.b64decode(
            b"v29zcGFyc2VTdHJ1Y3RNYXC/Y2Zvb79iaGlldGhlcmX/Y2Jher9iaGljYnll////"
        )
        actual_body: Any = (
            _cbor_loads(actual_body_content) if actual_body_content else {}
        )
        expected_body: Any = (
            _cbor_loads(expected_body_content) if expected_body_content else {}
        )
        assert deep_equal(actual_body, expected_body)

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


async def test_rpc_v2_cbor_serializes_null_map_values_request_rpc_v2_cbor_sparse_maps() -> (
    None
):
    """Serializes null map values in sparse maps"""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
        )
    )

    input_ = RpcV2CborSparseMapsInput(
        sparse_struct_map={"x": None},
        sparse_number_map={"x": None},
        sparse_boolean_map={"x": None},
        sparse_string_map={"x": None},
    )

    try:
        await client.rpc_v2_cbor_sparse_maps(input_)
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert (
            actual.destination.path
            == "/service/RpcV2Protocol/operation/RpcV2CborSparseMaps"
        )
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("accept", "application/cbor"),
            ("content-type", "application/cbor"),
            ("smithy-protocol", "rpc-v2-cbor"),
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = ["content-length"]
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = base64.b64decode(
            b"v3BzcGFyc2VCb29sZWFuTWFwv2F49v9vc3BhcnNlTnVtYmVyTWFwv2F49v9vc3BhcnNlU3RyaW5nTWFwv2F49v9vc3BhcnNlU3RydWN0TWFwv2F49v//"
        )
        actual_body: Any = (
            _cbor_loads(actual_body_content) if actual_body_content else {}
        )
        expected_body: Any = (
            _cbor_loads(expected_body_content) if expected_body_content else {}
        )
        assert deep_equal(actual_body, expected_body)

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


async def test_rpc_v2_cbor_serializes_sparse_set_map_request_rpc_v2_cbor_sparse_maps() -> (
    None
):
    """A request that contains a sparse map of sets"""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
        )
    )

    input_ = RpcV2CborSparseMapsInput(sparse_set_map={"x": [], "y": ["a", "b"]})

    try:
        await client.rpc_v2_cbor_sparse_maps(input_)
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert (
            actual.destination.path
            == "/service/RpcV2Protocol/operation/RpcV2CborSparseMaps"
        )
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("accept", "application/cbor"),
            ("content-type", "application/cbor"),
            ("smithy-protocol", "rpc-v2-cbor"),
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = ["content-length"]
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = base64.b64decode(
            b"v2xzcGFyc2VTZXRNYXC/YXif/2F5n2FhYWL///8="
        )
        actual_body: Any = (
            _cbor_loads(actual_body_content) if actual_body_content else {}
        )
        expected_body: Any = (
            _cbor_loads(expected_body_content) if expected_body_content else {}
        )
        assert deep_equal(actual_body, expected_body)

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


async def test_rpc_v2_cbor_serializes_sparse_set_map_and_retains_null_request_rpc_v2_cbor_sparse_maps() -> (
    None
):
    """A request that contains a sparse map of sets."""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
        )
    )

    input_ = RpcV2CborSparseMapsInput(
        sparse_set_map={"x": [], "y": ["a", "b"], "z": None}
    )

    try:
        await client.rpc_v2_cbor_sparse_maps(input_)
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert (
            actual.destination.path
            == "/service/RpcV2Protocol/operation/RpcV2CborSparseMaps"
        )
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("accept", "application/cbor"),
            ("content-type", "application/cbor"),
            ("smithy-protocol", "rpc-v2-cbor"),
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = ["content-length"]
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = base64.b64decode(
            b"v2xzcGFyc2VTZXRNYXC/YXif/2F5n2FhYWL/YXr2//8="
        )
        actual_body: Any = (
            _cbor_loads(actual_body_content) if actual_body_content else {}
        )
        expected_body: Any = (
            _cbor_loads(expected_body_content) if expected_body_content else {}
        )
        assert deep_equal(actual_body, expected_body)

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


async def test_rpc_v2_cbor_serializes_zero_values_in_sparse_maps_request_rpc_v2_cbor_sparse_maps() -> (
    None
):
    """Ensure that 0 and false are sent over the wire in all maps and lists"""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
        )
    )

    input_ = RpcV2CborSparseMapsInput(
        sparse_number_map={"x": 0}, sparse_boolean_map={"x": False}
    )

    try:
        await client.rpc_v2_cbor_sparse_maps(input_)
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert (
            actual.destination.path
            == "/service/RpcV2Protocol/operation/RpcV2CborSparseMaps"
        )
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("accept", "application/cbor"),
            ("content-type", "application/cbor"),
            ("smithy-protocol", "rpc-v2-cbor"),
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = ["content-length"]
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = base64.b64decode(
            b"v29zcGFyc2VOdW1iZXJNYXC/YXgA/3BzcGFyc2VCb29sZWFuTWFwv2F49P//"
        )
        actual_body: Any = (
            _cbor_loads(actual_body_content) if actual_body_content else {}
        )
        expected_body: Any = (
            _cbor_loads(expected_body_content) if expected_body_content else {}
        )
        assert deep_equal(actual_body, expected_body)

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


async def test_rpc_v2_cbor_sparse_json_maps_response_rpc_v2_cbor_sparse_maps() -> None:
    """Deserializes sparse maps"""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[
                    ("Content-Type", "application/cbor"),
                    ("smithy-protocol", "rpc-v2-cbor"),
                ],
                body=base64.b64decode(
                    b"v29zcGFyc2VTdHJ1Y3RNYXC/Y2Zvb79iaGlldGhlcmX/Y2Jher9iaGljYnll////"
                ),
            ),
        )
    )

    input_ = RpcV2CborSparseMapsInput()

    try:
        actual = await client.rpc_v2_cbor_sparse_maps(input_)
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = RpcV2CborSparseMapsOutput(
            sparse_struct_map={
                "foo": GreetingStruct(hi="there"),
                "baz": GreetingStruct(hi="bye"),
            }
        )

        assert deep_equal(actual, expected)


async def test_rpc_v2_cbor_deserializes_null_map_values_response_rpc_v2_cbor_sparse_maps() -> (
    None
):
    """Deserializes null map values"""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[
                    ("Content-Type", "application/cbor"),
                    ("smithy-protocol", "rpc-v2-cbor"),
                ],
                body=base64.b64decode(
                    b"v3BzcGFyc2VCb29sZWFuTWFwv2F49v9vc3BhcnNlTnVtYmVyTWFwv2F49v9vc3BhcnNlU3RyaW5nTWFwv2F49v9vc3BhcnNlU3RydWN0TWFwv2F49v//"
                ),
            ),
        )
    )

    input_ = RpcV2CborSparseMapsInput()

    try:
        actual = await client.rpc_v2_cbor_sparse_maps(input_)
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = RpcV2CborSparseMapsOutput(
            sparse_struct_map={"x": None},
            sparse_number_map={"x": None},
            sparse_boolean_map={"x": None},
            sparse_string_map={"x": None},
        )

        assert deep_equal(actual, expected)


async def test_rpc_v2_cbor_deserializes_sparse_set_map_response_rpc_v2_cbor_sparse_maps() -> (
    None
):
    """A response that contains a sparse map of sets"""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[
                    ("Content-Type", "application/cbor"),
                    ("smithy-protocol", "rpc-v2-cbor"),
                ],
                body=base64.b64decode(b"v2xzcGFyc2VTZXRNYXC/YXmfYWFhYv9heJ////8="),
            ),
        )
    )

    input_ = RpcV2CborSparseMapsInput()

    try:
        actual = await client.rpc_v2_cbor_sparse_maps(input_)
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = RpcV2CborSparseMapsOutput(sparse_set_map={"x": [], "y": ["a", "b"]})

        assert deep_equal(actual, expected)


async def test_rpc_v2_cbor_deserializes_sparse_set_map_and_retains_null_response_rpc_v2_cbor_sparse_maps() -> (
    None
):
    """A response that contains a sparse map of sets with a null"""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[
                    ("Content-Type", "application/cbor"),
                    ("smithy-protocol", "rpc-v2-cbor"),
                ],
                body=base64.b64decode(b"v2xzcGFyc2VTZXRNYXC/YXif/2F5n2FhYWL/YXr2//8="),
            ),
        )
    )

    input_ = RpcV2CborSparseMapsInput()

    try:
        actual = await client.rpc_v2_cbor_sparse_maps(input_)
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = RpcV2CborSparseMapsOutput(
            sparse_set_map={"x": [], "y": ["a", "b"], "z": None}
        )

        assert deep_equal(actual, expected)


async def test_rpc_v2_cbor_deserializes_zero_values_in_sparse_maps_response_rpc_v2_cbor_sparse_maps() -> (
    None
):
    """Ensure that 0 and false are sent over the wire in all maps and lists"""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[
                    ("Content-Type", "application/cbor"),
                    ("smithy-protocol", "rpc-v2-cbor"),
                ],
                body=base64.b64decode(
                    b"v29zcGFyc2VOdW1iZXJNYXC/YXgA/3BzcGFyc2VCb29sZWFuTWFwv2F49P//"
                ),
            ),
        )
    )

    input_ = RpcV2CborSparseMapsInput()

    try:
        actual = await client.rpc_v2_cbor_sparse_maps(input_)
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = RpcV2CborSparseMapsOutput(
            sparse_number_map={"x": 0}, sparse_boolean_map={"x": False}
        )

        assert deep_equal(actual, expected)


async def test_rpc_v2_cbor_serializes_union_value_request_rpc_v2_cbor_unions() -> None:
    """Serializes a union followed by another structure member"""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
        )
    )

    input_ = RpcV2CborUnionsInput(
        contents=RpcV2CborUnionStringValue(value="foo"), other_value="bar"
    )

    try:
        await client.rpc_v2_cbor_unions(input_)
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert (
            actual.destination.path
            == "/service/RpcV2Protocol/operation/RpcV2CborUnions"
        )
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("accept", "application/cbor"),
            ("content-type", "application/cbor"),
            ("smithy-protocol", "rpc-v2-cbor"),
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = ["content-length"]
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = base64.b64decode(
            b"omhjb250ZW50c6Frc3RyaW5nVmFsdWVjZm9vam90aGVyVmFsdWVjYmFy"
        )
        actual_body: Any = (
            _cbor_loads(actual_body_content) if actual_body_content else {}
        )
        expected_body: Any = (
            _cbor_loads(expected_body_content) if expected_body_content else {}
        )
        assert deep_equal(actual_body, expected_body)

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


async def test_rpc_v2_cbor_serializes_nested_union_value_request_rpc_v2_cbor_unions() -> (
    None
):
    """Serializes a nested union followed by another structure member"""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
        )
    )

    input_ = RpcV2CborUnionsInput(
        contents=RpcV2CborUnionUnionValue(
            value=RpcV2CborNestedUnionStringValue(value="foo")
        ),
        other_value="bar",
    )

    try:
        await client.rpc_v2_cbor_unions(input_)
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert (
            actual.destination.path
            == "/service/RpcV2Protocol/operation/RpcV2CborUnions"
        )
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("accept", "application/cbor"),
            ("content-type", "application/cbor"),
            ("smithy-protocol", "rpc-v2-cbor"),
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = ["content-length"]
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = base64.b64decode(
            b"omhjb250ZW50c6FqdW5pb25WYWx1ZaFrc3RyaW5nVmFsdWVjZm9vam90aGVyVmFsdWVjYmFy"
        )
        actual_body: Any = (
            _cbor_loads(actual_body_content) if actual_body_content else {}
        )
        expected_body: Any = (
            _cbor_loads(expected_body_content) if expected_body_content else {}
        )
        assert deep_equal(actual_body, expected_body)

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


async def test_rpc_v2_cbor_deserializes_union_value_response_rpc_v2_cbor_unions() -> (
    None
):
    """Deserializes a tagged union followed by another structure member"""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[
                    ("Content-Type", "application/cbor"),
                    ("smithy-protocol", "rpc-v2-cbor"),
                ],
                body=base64.b64decode(
                    b"omhjb250ZW50c6Frc3RyaW5nVmFsdWVjZm9vam90aGVyVmFsdWVjYmFy"
                ),
            ),
        )
    )

    input_ = RpcV2CborUnionsInput()

    try:
        actual = await client.rpc_v2_cbor_unions(input_)
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = RpcV2CborUnionsOutput(
            contents=RpcV2CborUnionStringValue(value="foo"), other_value="bar"
        )

        assert deep_equal(actual, expected)


async def test_rpc_v2_cbor_deserializes_nested_union_value_response_rpc_v2_cbor_unions() -> (
    None
):
    """Deserializes a nested union followed by another structure member"""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[
                    ("Content-Type", "application/cbor"),
                    ("smithy-protocol", "rpc-v2-cbor"),
                ],
                body=base64.b64decode(
                    b"omhjb250ZW50c6FqdW5pb25WYWx1ZaFrc3RyaW5nVmFsdWVjZm9vam90aGVyVmFsdWVjYmFy"
                ),
            ),
        )
    )

    input_ = RpcV2CborUnionsInput()

    try:
        actual = await client.rpc_v2_cbor_unions(input_)
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = RpcV2CborUnionsOutput(
            contents=RpcV2CborUnionUnionValue(
                value=RpcV2CborNestedUnionStringValue(value="foo")
            ),
            other_value="bar",
        )

        assert deep_equal(actual, expected)


async def test_rpc_v2_cbor_simple_scalar_properties_request_simple_scalar_properties() -> (
    None
):
    """Serializes simple scalar properties"""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
        )
    )

    input_ = SimpleScalarPropertiesInput(
        true_boolean_value=True,
        false_boolean_value=False,
        byte_value=5,
        double_value=float(1.889),
        float_value=float(7.625),
        integer_value=256,
        long_value=9873,
        short_value=9898,
        string_value="simple",
        blob_value=b"foo",
    )

    try:
        await client.simple_scalar_properties(input_)
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert (
            actual.destination.path
            == "/service/RpcV2Protocol/operation/SimpleScalarProperties"
        )
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("accept", "application/cbor"),
            ("content-type", "application/cbor"),
            ("smithy-protocol", "rpc-v2-cbor"),
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = ["content-length"]
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = base64.b64decode(
            b"v2lieXRlVmFsdWUFa2RvdWJsZVZhbHVl+z/+OVgQYk3TcWZhbHNlQm9vbGVhblZhbHVl9GpmbG9hdFZhbHVl+kD0AABsaW50ZWdlclZhbHVlGQEAaWxvbmdWYWx1ZRkmkWpzaG9ydFZhbHVlGSaqa3N0cmluZ1ZhbHVlZnNpbXBsZXB0cnVlQm9vbGVhblZhbHVl9WlibG9iVmFsdWVDZm9v/w=="
        )
        actual_body: Any = (
            _cbor_loads(actual_body_content) if actual_body_content else {}
        )
        expected_body: Any = (
            _cbor_loads(expected_body_content) if expected_body_content else {}
        )
        assert deep_equal(actual_body, expected_body)

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


async def test_rpc_v2_cbor_client_doesnt_serialize_null_structure_values_request_simple_scalar_properties() -> (
    None
):
    """RpcV2 Cbor should not serialize null structure values"""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
        )
    )

    input_ = SimpleScalarPropertiesInput(string_value=None)

    try:
        await client.simple_scalar_properties(input_)
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert (
            actual.destination.path
            == "/service/RpcV2Protocol/operation/SimpleScalarProperties"
        )
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("accept", "application/cbor"),
            ("content-type", "application/cbor"),
            ("smithy-protocol", "rpc-v2-cbor"),
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = ["content-length"]
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = base64.b64decode(b"v/8=")
        actual_body: Any = (
            _cbor_loads(actual_body_content) if actual_body_content else {}
        )
        expected_body: Any = (
            _cbor_loads(expected_body_content) if expected_body_content else {}
        )
        assert deep_equal(actual_body, expected_body)

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


async def test_rpc_v2_cbor_supports_na_n_float_inputs_request_simple_scalar_properties() -> (
    None
):
    """Supports handling NaN float values."""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
        )
    )

    input_ = SimpleScalarPropertiesInput(
        double_value=float("nan"), float_value=float("nan")
    )

    try:
        await client.simple_scalar_properties(input_)
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert (
            actual.destination.path
            == "/service/RpcV2Protocol/operation/SimpleScalarProperties"
        )
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("accept", "application/cbor"),
            ("content-type", "application/cbor"),
            ("smithy-protocol", "rpc-v2-cbor"),
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = ["content-length"]
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = base64.b64decode(
            b"v2tkb3VibGVWYWx1Zft/+AAAAAAAAGpmbG9hdFZhbHVl+n/AAAD/"
        )
        actual_body: Any = (
            _cbor_loads(actual_body_content) if actual_body_content else {}
        )
        expected_body: Any = (
            _cbor_loads(expected_body_content) if expected_body_content else {}
        )
        assert deep_equal(actual_body, expected_body)

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


async def test_rpc_v2_cbor_supports_infinity_float_inputs_request_simple_scalar_properties() -> (
    None
):
    """Supports handling Infinity float values."""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
        )
    )

    input_ = SimpleScalarPropertiesInput(
        double_value=float("inf"), float_value=float("inf")
    )

    try:
        await client.simple_scalar_properties(input_)
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert (
            actual.destination.path
            == "/service/RpcV2Protocol/operation/SimpleScalarProperties"
        )
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("accept", "application/cbor"),
            ("content-type", "application/cbor"),
            ("smithy-protocol", "rpc-v2-cbor"),
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = ["content-length"]
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = base64.b64decode(
            b"v2tkb3VibGVWYWx1Zft/8AAAAAAAAGpmbG9hdFZhbHVl+n+AAAD/"
        )
        actual_body: Any = (
            _cbor_loads(actual_body_content) if actual_body_content else {}
        )
        expected_body: Any = (
            _cbor_loads(expected_body_content) if expected_body_content else {}
        )
        assert deep_equal(actual_body, expected_body)

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


async def test_rpc_v2_cbor_supports_negative_infinity_float_inputs_request_simple_scalar_properties() -> (
    None
):
    """Supports handling Infinity float values."""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
        )
    )

    input_ = SimpleScalarPropertiesInput(
        double_value=float("-inf"), float_value=float("-inf")
    )

    try:
        await client.simple_scalar_properties(input_)
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert (
            actual.destination.path
            == "/service/RpcV2Protocol/operation/SimpleScalarProperties"
        )
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("accept", "application/cbor"),
            ("content-type", "application/cbor"),
            ("smithy-protocol", "rpc-v2-cbor"),
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = ["content-length"]
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = base64.b64decode(
            b"v2tkb3VibGVWYWx1Zfv/8AAAAAAAAGpmbG9hdFZhbHVl+v+AAAD/"
        )
        actual_body: Any = (
            _cbor_loads(actual_body_content) if actual_body_content else {}
        )
        expected_body: Any = (
            _cbor_loads(expected_body_content) if expected_body_content else {}
        )
        assert deep_equal(actual_body, expected_body)

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


async def test_rpc_v2_cbor_simple_scalar_properties_response_simple_scalar_properties() -> (
    None
):
    """Serializes simple scalar properties"""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[
                    ("Content-Type", "application/cbor"),
                    ("smithy-protocol", "rpc-v2-cbor"),
                ],
                body=base64.b64decode(
                    b"v3B0cnVlQm9vbGVhblZhbHVl9XFmYWxzZUJvb2xlYW5WYWx1ZfRpYnl0ZVZhbHVlBWtkb3VibGVWYWx1Zfs//jlYEGJN02pmbG9hdFZhbHVl+kD0AABsaW50ZWdlclZhbHVlGQEAanNob3J0VmFsdWUZJqprc3RyaW5nVmFsdWVmc2ltcGxlaWJsb2JWYWx1ZUNmb2//"
                ),
            ),
        )
    )

    input_ = SimpleScalarPropertiesInput()

    try:
        actual = await client.simple_scalar_properties(input_)
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = SimpleScalarPropertiesOutput(
            true_boolean_value=True,
            false_boolean_value=False,
            byte_value=5,
            double_value=float(1.889),
            float_value=float(7.625),
            integer_value=256,
            short_value=9898,
            string_value="simple",
            blob_value=b"foo",
        )

        assert deep_equal(actual, expected)


async def test_rpc_v2_cbor_simple_scalar_properties_using_definite_length_response_simple_scalar_properties() -> (
    None
):
    """
    Deserializes simple scalar properties encoded using a map with definite
    length
    """
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[
                    ("Content-Type", "application/cbor"),
                    ("smithy-protocol", "rpc-v2-cbor"),
                ],
                body=base64.b64decode(
                    b"qXB0cnVlQm9vbGVhblZhbHVl9XFmYWxzZUJvb2xlYW5WYWx1ZfRpYnl0ZVZhbHVlBWtkb3VibGVWYWx1Zfs//jlYEGJN02pmbG9hdFZhbHVl+kD0AABsaW50ZWdlclZhbHVlGQEAanNob3J0VmFsdWUZJqprc3RyaW5nVmFsdWVmc2ltcGxlaWJsb2JWYWx1ZUNmb28="
                ),
            ),
        )
    )

    input_ = SimpleScalarPropertiesInput()

    try:
        actual = await client.simple_scalar_properties(input_)
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = SimpleScalarPropertiesOutput(
            true_boolean_value=True,
            false_boolean_value=False,
            byte_value=5,
            double_value=float(1.889),
            float_value=float(7.625),
            integer_value=256,
            short_value=9898,
            string_value="simple",
            blob_value=b"foo",
        )

        assert deep_equal(actual, expected)


async def test_rpc_v2_cbor_client_doesnt_deserialize_null_structure_values_response_simple_scalar_properties() -> (
    None
):
    """RpcV2 Cbor should not deserialize null structure values"""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[
                    ("Content-Type", "application/cbor"),
                    ("smithy-protocol", "rpc-v2-cbor"),
                ],
                body=base64.b64decode(b"v2tzdHJpbmdWYWx1Zfb/"),
            ),
        )
    )

    input_ = SimpleScalarPropertiesInput()

    try:
        actual = await client.simple_scalar_properties(input_)
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = SimpleScalarPropertiesOutput()

        assert deep_equal(actual, expected)


async def test_rpc_v2_cbor_supports_na_n_float_outputs_response_simple_scalar_properties() -> (
    None
):
    """Supports handling NaN float values."""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[
                    ("Content-Type", "application/cbor"),
                    ("smithy-protocol", "rpc-v2-cbor"),
                ],
                body=base64.b64decode(
                    b"v2tkb3VibGVWYWx1Zft/+AAAAAAAAGpmbG9hdFZhbHVl+n/AAAD/"
                ),
            ),
        )
    )

    input_ = SimpleScalarPropertiesInput()

    try:
        actual = await client.simple_scalar_properties(input_)
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = SimpleScalarPropertiesOutput(
            double_value=float("nan"), float_value=float("nan")
        )

        assert deep_equal(actual, expected)


async def test_rpc_v2_cbor_supports_infinity_float_outputs_response_simple_scalar_properties() -> (
    None
):
    """Supports handling Infinity float values."""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[
                    ("Content-Type", "application/cbor"),
                    ("smithy-protocol", "rpc-v2-cbor"),
                ],
                body=base64.b64decode(
                    b"v2tkb3VibGVWYWx1Zft/8AAAAAAAAGpmbG9hdFZhbHVl+n+AAAD/"
                ),
            ),
        )
    )

    input_ = SimpleScalarPropertiesInput()

    try:
        actual = await client.simple_scalar_properties(input_)
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = SimpleScalarPropertiesOutput(
            double_value=float("inf"), float_value=float("inf")
        )

        assert deep_equal(actual, expected)


async def test_rpc_v2_cbor_supports_negative_infinity_float_outputs_response_simple_scalar_properties() -> (
    None
):
    """Supports handling Negative Infinity float values."""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[
                    ("Content-Type", "application/cbor"),
                    ("smithy-protocol", "rpc-v2-cbor"),
                ],
                body=base64.b64decode(
                    b"v2tkb3VibGVWYWx1Zfv/8AAAAAAAAGpmbG9hdFZhbHVl+v+AAAD/"
                ),
            ),
        )
    )

    input_ = SimpleScalarPropertiesInput()

    try:
        actual = await client.simple_scalar_properties(input_)
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = SimpleScalarPropertiesOutput(
            double_value=float("-inf"), float_value=float("-inf")
        )

        assert deep_equal(actual, expected)


async def test_rpc_v2_cbor_supports_upcasting_data_on_deserialize_response_simple_scalar_properties() -> (
    None
):
    """
    Supports upcasting from a smaller byte representation of the same data
    type.
    """
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[
                    ("Content-Type", "application/cbor"),
                    ("smithy-protocol", "rpc-v2-cbor"),
                ],
                body=base64.b64decode(
                    b"v2tkb3VibGVWYWx1Zfk+AGpmbG9hdFZhbHVl+UegbGludGVnZXJWYWx1ZRg4aWxvbmdWYWx1ZRkBAGpzaG9ydFZhbHVlCv8="
                ),
            ),
        )
    )

    input_ = SimpleScalarPropertiesInput()

    try:
        actual = await client.simple_scalar_properties(input_)
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = SimpleScalarPropertiesOutput(
            double_value=float(1.5),
            float_value=float(7.625),
            integer_value=56,
            long_value=256,
            short_value=10,
        )

        assert deep_equal(actual, expected)


async def test_rpc_v2_cbor_extra_fields_in_the_body_should_be_skipped_by_clients_response_simple_scalar_properties() -> (
    None
):
    """
    The client should skip over additional fields that are not part of the
    structure. This allows a client generated against an older Smithy model
    to be able to communicate with a server that is generated against a
    newer Smithy model.
    """
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[
                    ("Content-Type", "application/cbor"),
                    ("smithy-protocol", "rpc-v2-cbor"),
                ],
                body=base64.b64decode(
                    b""
                ),
            ),
        )
    )

    input_ = SimpleScalarPropertiesInput()

    try:
        actual = await client.simple_scalar_properties(input_)
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = SimpleScalarPropertiesOutput(
            true_boolean_value=True,
            false_boolean_value=False,
            byte_value=5,
            double_value=float(1.889),
            float_value=float(7.625),
            integer_value=256,
            long_value=9873,
            short_value=9898,
            string_value="simple",
            blob_value=b"foo",
        )

        assert deep_equal(actual, expected)


async def test_rpc_v2_cbor_sparse_maps_serialize_null_values_request_sparse_nulls_operation() -> (
    None
):
    """Serializes null values in maps"""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
        )
    )

    input_ = SparseNullsOperationInput(sparse_string_map={"foo": None})

    try:
        await client.sparse_nulls_operation(input_)
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert (
            actual.destination.path
            == "/service/RpcV2Protocol/operation/SparseNullsOperation"
        )
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("accept", "application/cbor"),
            ("content-type", "application/cbor"),
            ("smithy-protocol", "rpc-v2-cbor"),
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = ["content-length"]
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = base64.b64decode(
            b"v29zcGFyc2VTdHJpbmdNYXC/Y2Zvb/b//w=="
        )
        actual_body: Any = (
            _cbor_loads(actual_body_content) if actual_body_content else {}
        )
        expected_body: Any = (
            _cbor_loads(expected_body_content) if expected_body_content else {}
        )
        assert deep_equal(actual_body, expected_body)

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


async def test_rpc_v2_cbor_sparse_lists_serialize_null_request_sparse_nulls_operation() -> (
    None
):
    """Serializes null values in lists"""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
        )
    )

    input_ = SparseNullsOperationInput(sparse_string_list=[None])

    try:
        await client.sparse_nulls_operation(input_)
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert (
            actual.destination.path
            == "/service/RpcV2Protocol/operation/SparseNullsOperation"
        )
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("accept", "application/cbor"),
            ("content-type", "application/cbor"),
            ("smithy-protocol", "rpc-v2-cbor"),
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = ["content-length"]
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = base64.b64decode(b"v3BzcGFyc2VTdHJpbmdMaXN0n/b//w==")
        actual_body: Any = (
            _cbor_loads(actual_body_content) if actual_body_content else {}
        )
        expected_body: Any = (
            _cbor_loads(expected_body_content) if expected_body_content else {}
        )
        assert deep_equal(actual_body, expected_body)

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


async def test_rpc_v2_cbor_sparse_maps_deserialize_null_values_response_sparse_nulls_operation() -> (
    None
):
    """Deserializes null values in maps"""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[
                    ("Content-Type", "application/cbor"),
                    ("smithy-protocol", "rpc-v2-cbor"),
                ],
                body=base64.b64decode(b"v29zcGFyc2VTdHJpbmdNYXC/Y2Zvb/b//w=="),
            ),
        )
    )

    input_ = SparseNullsOperationInput()

    try:
        actual = await client.sparse_nulls_operation(input_)
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = SparseNullsOperationOutput(sparse_string_map={"foo": None})

        assert deep_equal(actual, expected)


async def test_rpc_v2_cbor_sparse_lists_deserialize_null_response_sparse_nulls_operation() -> (
    None
):
    """Deserializes null values in lists"""
    client = RpcV2Protocol(
        config=Config(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[
                    ("Content-Type", "application/cbor"),
                    ("smithy-protocol", "rpc-v2-cbor"),
                ],
                body=base64.b64decode(b"v3BzcGFyc2VTdHJpbmdMaXN0n/b//w=="),
            ),
        )
    )

    input_ = SparseNullsOperationInput()

    try:
        actual = await client.sparse_nulls_operation(input_)
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = SparseNullsOperationOutput(sparse_string_list=[None])

        assert deep_equal(actual, expected)


class TestHttpServiceError(ServiceError):
    """A test error that subclasses the service-error for protocol tests."""

    def __init__(self, request: HTTPRequest):
        self.request = request


class RequestTestHTTPClient:
    """An asynchronous HTTP client solely for testing purposes."""

    TIMEOUT_EXCEPTIONS = ()
    SUPPORTS_DUPLEX_STREAMING: bool = True

    def __init__(self, *, client_config: HTTPClientConfiguration | None = None):
        self._client_config = client_config

    async def send(
        self,
        request: HTTPRequest,
        *,
        request_config: HTTPRequestConfiguration | None = None,
    ) -> _smithy_http_aio_interfaces_HTTPResponse:
        # Raise the exception with the request object to bypass actual request handling
        raise TestHttpServiceError(request)


class ResponseTestHTTPClient:
    """An asynchronous HTTP client solely for testing purposes."""

    TIMEOUT_EXCEPTIONS = ()
    SUPPORTS_DUPLEX_STREAMING: bool = True

    def __init__(
        self,
        *,
        client_config: HTTPClientConfiguration | None = None,
        status: int = 200,
        headers: list[tuple[str, str]] | None = None,
        body: bytes = b"",
    ):
        self._client_config = client_config
        self.status = status
        self.fields = tuples_to_fields(headers or [])
        self.body = body

    async def send(
        self,
        request: HTTPRequest,
        *,
        request_config: HTTPRequestConfiguration | None = None,
    ) -> _smithy_http_aio_HTTPResponse:
        # Pre-construct the response from the request and return it
        return _smithy_http_aio_HTTPResponse(
            status=self.status, fields=self.fields, body=async_list([self.body])
        )
