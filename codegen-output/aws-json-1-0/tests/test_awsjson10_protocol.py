# Code generated by smithy-python-codegen DO NOT EDIT.

from datetime import datetime, timezone
import json
from urllib.parse import parse_qsl

from pytest import fail, mark, raises
from smithy_aws_core.identity import StaticCredentialsResolver
from smithy_core.aio.retries import SimpleRetryStrategy
from smithy_core.aio.types import AsyncBytesReader
from smithy_core.aio.utils import async_list
from smithy_core.documents import Document
from smithy_http import tuples_to_fields
from smithy_http.aio import HTTPResponse as _smithy_http_aio_HTTPResponse
from smithy_http.aio.interfaces import (
    HTTPRequest,
    HTTPResponse as _smithy_http_aio_interfaces_HTTPResponse,
)
from smithy_http.interfaces import HTTPClientConfiguration, HTTPRequestConfiguration

from awsjson10.client import AsyncJSONRPC10Client
from awsjson10.config import AsyncJSONRPC10Config
from awsjson10.models import (
    ClientOptionalDefaults,
    Defaults,
    Dialog,
    EmptyInputAndEmptyOutputOutput,
    Farewell,
    GreetingStruct,
    JsonUnionsOutput,
    MyUnionBlobValue,
    MyUnionBooleanValue,
    MyUnionEnumValue,
    MyUnionIntEnumValue,
    MyUnionListValue,
    MyUnionMapValue,
    MyUnionNumberValue,
    MyUnionStringValue,
    MyUnionStructureValue,
    MyUnionTimestampValue,
    NoInputAndNoOutputOutput,
    NoInputAndOutputOutput,
    OperationWithDefaultsOutput,
    OperationWithNestedStructureOutput,
    OperationWithRequiredMembersOutput,
    OperationWithRequiredMembersWithDefaultsOutput,
    ServiceError,
    SimpleScalarPropertiesOutput,
    TopLevel,
)


async def test_aws_json10_empty_input_and_empty_output_request_empty_input_and_empty_output() -> (
    None
):
    """Clients must always send an empty object if input is modeled."""
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        await client.empty_input_and_empty_output()
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert actual.destination.path == "/"
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("content-type", "application/x-amz-json-1.0"),
            ("x-amz-target", "JsonRpc10.EmptyInputAndEmptyOutput"),
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = []
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = b"{}"
        actual_body = json.loads(actual_body_content) if actual_body_content else ""
        expected_body = json.loads(expected_body_content)
        assert actual_body == expected_body

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


async def test_aws_json10_empty_input_and_empty_output_send_json_object_response_empty_input_and_empty_output() -> (
    None
):
    """
    A service will always return a JSON object for operations with modeled
    output.
    """
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[("Content-Type", "application/x-amz-json-1.0")],
                body=b"{}",
            ),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        actual = await client.empty_input_and_empty_output()
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = EmptyInputAndEmptyOutputOutput()

        assert actual == expected


@mark.xfail()
async def test_aws_json10_endpoint_trait_request_endpoint_operation() -> None:
    """
    Operations can prepend to the given host if they define the endpoint
    trait.
    """
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        await client.endpoint_operation()
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert actual.destination.path == "/"
        assert actual.destination.host == "foo.example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = []
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = []
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = b"{}"
        assert actual_body_content == expected_body_content

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


@mark.xfail()
async def test_aws_json10_endpoint_trait_with_host_label_request_endpoint_with_host_label_operation() -> (
    None
):
    """
    Operations can prepend to the given host if they define the endpoint
    trait, and can use the host label trait to define further customization
    based on user input.
    """
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        await client.endpoint_with_host_label_operation(label="bar")
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert actual.destination.path == "/"
        assert actual.destination.host == "foo.bar.example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = []
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = []
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = b'{"label": "bar"}'
        actual_body = json.loads(actual_body_content) if actual_body_content else ""
        expected_body = json.loads(expected_body_content)
        assert actual_body == expected_body

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


async def test_aws_json10_invalid_greeting_error_error_greeting_with_errors() -> None:
    """Parses simple JSON errors"""
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=400,
                headers=[("Content-Type", "application/x-amz-json-1.0")],
                body=b'{\n    "__type": "aws.protocoltests.json10#InvalidGreeting",\n    "Message": "Hi"\n}',
            ),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        await client.greeting_with_errors()
        fail("Expected 'InvalidGreeting' exception to be thrown!")
    except Exception as err:
        if type(err).__name__ != "InvalidGreeting":
            fail(
                f"Expected 'InvalidGreeting' exception to be thrown, but received {type(err).__name__}: {err}"
            )


async def test_aws_json10_complex_error_error_greeting_with_errors() -> None:
    """Parses a complex error with no message member"""
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=400,
                headers=[("Content-Type", "application/x-amz-json-1.0")],
                body=b'{\n    "__type": "aws.protocoltests.json10#ComplexError",\n    "TopLevel": "Top level",\n    "Nested": {\n        "Foo": "bar"\n    }\n}',
            ),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        await client.greeting_with_errors()
        fail("Expected 'ComplexError' exception to be thrown!")
    except Exception as err:
        if type(err).__name__ != "ComplexError":
            fail(
                f"Expected 'ComplexError' exception to be thrown, but received {type(err).__name__}: {err}"
            )


async def test_aws_json10_empty_complex_error_error_greeting_with_errors() -> None:
    """Parses a complex error with an empty body"""
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=400,
                headers=[("Content-Type", "application/x-amz-json-1.0")],
                body=b'{\n    "__type": "aws.protocoltests.json10#ComplexError"\n}',
            ),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        await client.greeting_with_errors()
        fail("Expected 'ComplexError' exception to be thrown!")
    except Exception as err:
        if type(err).__name__ != "ComplexError":
            fail(
                f"Expected 'ComplexError' exception to be thrown, but received {type(err).__name__}: {err}"
            )


async def test_aws_json10_foo_error_using_x_amzn_error_type_error_greeting_with_errors() -> (
    None
):
    """
    Serializes the X-Amzn-ErrorType header. For an example service, see
    Amazon EKS.
    """
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=500, headers=[("X-Amzn-Errortype", "FooError")], body=b""
            ),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        await client.greeting_with_errors()
        fail("Expected 'FooError' exception to be thrown!")
    except Exception as err:
        if type(err).__name__ != "FooError":
            fail(
                f"Expected 'FooError' exception to be thrown, but received {type(err).__name__}: {err}"
            )


async def test_aws_json10_foo_error_using_x_amzn_error_type_with_uri_error_greeting_with_errors() -> (
    None
):
    """
    Some X-Amzn-Errortype headers contain URLs. Clients need to split the
    URL on ':' and take only the first half of the string. For example,
    'ValidationException:http://internal.amazon.com/coral/com.amazon.coral.validate/'
    is to be interpreted as 'ValidationException'. For an example service
    see Amazon Polly.
    """
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=500,
                headers=[
                    (
                        "X-Amzn-Errortype",
                        "FooError:http://internal.amazon.com/coral/com.amazon.coral.validate/",
                    )
                ],
                body=b"",
            ),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        await client.greeting_with_errors()
        fail("Expected 'FooError' exception to be thrown!")
    except Exception as err:
        if type(err).__name__ != "FooError":
            fail(
                f"Expected 'FooError' exception to be thrown, but received {type(err).__name__}: {err}"
            )


async def test_aws_json10_foo_error_using_x_amzn_error_type_with_uri_and_namespace_error_greeting_with_errors() -> (
    None
):
    """
    X-Amzn-Errortype might contain a URL and a namespace. Client should
    extract only the shape name. This is a pathalogical case that might not
    actually happen in any deployed AWS service.
    """
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=500,
                headers=[
                    (
                        "X-Amzn-Errortype",
                        "aws.protocoltests.json10#FooError:http://internal.amazon.com/coral/com.amazon.coral.validate/",
                    )
                ],
                body=b"",
            ),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        await client.greeting_with_errors()
        fail("Expected 'FooError' exception to be thrown!")
    except Exception as err:
        if type(err).__name__ != "FooError":
            fail(
                f"Expected 'FooError' exception to be thrown, but received {type(err).__name__}: {err}"
            )


async def test_aws_json10_foo_error_using_code_error_greeting_with_errors() -> None:
    """
    This example uses the 'code' property in the output rather than
    X-Amzn-Errortype. Some services do this though it's preferable to send
    the X-Amzn-Errortype. Client implementations must first check for the
    X-Amzn-Errortype and then check for a top-level 'code' property. For
    example service see Amazon S3 Glacier.
    """
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=500,
                headers=[("Content-Type", "application/x-amz-json-1.0")],
                body=b'{\n    "code": "FooError"\n}',
            ),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        await client.greeting_with_errors()
        fail("Expected 'FooError' exception to be thrown!")
    except Exception as err:
        if type(err).__name__ != "FooError":
            fail(
                f"Expected 'FooError' exception to be thrown, but received {type(err).__name__}: {err}"
            )


async def test_aws_json10_foo_error_using_code_and_namespace_error_greeting_with_errors() -> (
    None
):
    """
    Some services serialize errors using code, and it might contain a
    namespace. Clients should just take the last part of the string after
    '#'.
    """
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=500,
                headers=[("Content-Type", "application/x-amz-json-1.0")],
                body=b'{\n    "code": "aws.protocoltests.json10#FooError"\n}',
            ),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        await client.greeting_with_errors()
        fail("Expected 'FooError' exception to be thrown!")
    except Exception as err:
        if type(err).__name__ != "FooError":
            fail(
                f"Expected 'FooError' exception to be thrown, but received {type(err).__name__}: {err}"
            )


async def test_aws_json10_foo_error_using_code_uri_and_namespace_error_greeting_with_errors() -> (
    None
):
    """
    Some services serialize errors using code, and it might contain a
    namespace. It also might contain a URI. Clients should just take the
    last part of the string after '#' and before \":\". This is a
    pathalogical case that might not occur in any deployed AWS service.
    """
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=500,
                headers=[("Content-Type", "application/x-amz-json-1.0")],
                body=b'{\n    "code": "aws.protocoltests.json10#FooError:http://internal.amazon.com/coral/com.amazon.coral.validate/"\n}',
            ),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        await client.greeting_with_errors()
        fail("Expected 'FooError' exception to be thrown!")
    except Exception as err:
        if type(err).__name__ != "FooError":
            fail(
                f"Expected 'FooError' exception to be thrown, but received {type(err).__name__}: {err}"
            )


async def test_aws_json10_foo_error_with_dunder_type_error_greeting_with_errors() -> (
    None
):
    """Some services serialize errors using __type."""
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=500,
                headers=[("Content-Type", "application/x-amz-json-1.0")],
                body=b'{\n    "__type": "FooError"\n}',
            ),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        await client.greeting_with_errors()
        fail("Expected 'FooError' exception to be thrown!")
    except Exception as err:
        if type(err).__name__ != "FooError":
            fail(
                f"Expected 'FooError' exception to be thrown, but received {type(err).__name__}: {err}"
            )


async def test_aws_json10_foo_error_with_dunder_type_and_namespace_error_greeting_with_errors() -> (
    None
):
    """
    Some services serialize errors using __type, and it might contain a
    namespace. Clients should just take the last part of the string after
    '#'.
    """
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=500,
                headers=[("Content-Type", "application/x-amz-json-1.0")],
                body=b'{\n    "__type": "aws.protocoltests.json10#FooError"\n}',
            ),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        await client.greeting_with_errors()
        fail("Expected 'FooError' exception to be thrown!")
    except Exception as err:
        if type(err).__name__ != "FooError":
            fail(
                f"Expected 'FooError' exception to be thrown, but received {type(err).__name__}: {err}"
            )


async def test_aws_json10_foo_error_with_dunder_type_and_different_namespace_error_greeting_with_errors() -> (
    None
):
    """
    Because only the part after '#' is considered, an unrecognized
    namespace should not make a difference.
    """
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=500,
                headers=[("Content-Type", "application/x-amz-json-1.0")],
                body=b'{\n    "__type": "aws.different.namespace#FooError"\n}',
            ),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        await client.greeting_with_errors()
        fail("Expected 'FooError' exception to be thrown!")
    except Exception as err:
        if type(err).__name__ != "FooError":
            fail(
                f"Expected 'FooError' exception to be thrown, but received {type(err).__name__}: {err}"
            )


async def test_aws_json10_foo_error_with_dunder_type_uri_and_namespace_error_greeting_with_errors() -> (
    None
):
    """
    Some services serialize errors using __type, and it might contain a
    namespace. It also might contain a URI. Clients should just take the
    last part of the string after '#' and before \":\". This is a
    pathalogical case that might not occur in any deployed AWS service.
    """
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=500,
                headers=[("Content-Type", "application/x-amz-json-1.0")],
                body=b'{\n    "__type": "aws.protocoltests.json10#FooError:http://internal.amazon.com/coral/com.amazon.coral.validate/"\n}',
            ),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        await client.greeting_with_errors()
        fail("Expected 'FooError' exception to be thrown!")
    except Exception as err:
        if type(err).__name__ != "FooError":
            fail(
                f"Expected 'FooError' exception to be thrown, but received {type(err).__name__}: {err}"
            )


async def test_aws_json10_foo_error_with_nested_type_property_error_greeting_with_errors() -> (
    None
):
    """
    Some services serialize errors using __type, and if the response
    includes additional shapes that belong to a different namespace
    there'll be a nested __type property that must not be considered when
    determining which error to be surfaced. For an example service see
    Amazon DynamoDB.
    """
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=500,
                headers=[("Content-Type", "application/x-amz-json-1.0")],
                body=b'{\n    "__type": "aws.protocoltests.json10#FooError",\n    "ErrorDetails": [\n      {\n          "__type": "com.amazon.internal#ErrorDetails",\n          "reason": "Some reason"\n      }\n    ]\n}',
            ),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        await client.greeting_with_errors()
        fail("Expected 'FooError' exception to be thrown!")
    except Exception as err:
        if type(err).__name__ != "FooError":
            fail(
                f"Expected 'FooError' exception to be thrown, but received {type(err).__name__}: {err}"
            )


async def test_aws_json10_host_with_path_request_host_with_path_operation() -> None:
    """Custom endpoints supplied by users can have paths"""
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com/custom",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        await client.host_with_path_operation()
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert actual.destination.path == "/custom/"
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = []
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = []
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = b"{}"
        assert actual_body_content == expected_body_content

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


async def test_aws_json10_serialize_string_union_value_request_json_unions() -> None:
    """Serializes a string union value"""
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        await client.json_unions(contents=MyUnionStringValue(value="foo"))
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert actual.destination.path == "/"
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("content-type", "application/x-amz-json-1.0"),
            ("x-amz-target", "JsonRpc10.JsonUnions"),
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = []
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = (
            b'{\n    "contents": {\n        "stringValue": "foo"\n    }\n}'
        )
        actual_body = json.loads(actual_body_content) if actual_body_content else ""
        expected_body = json.loads(expected_body_content)
        assert actual_body == expected_body

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


async def test_aws_json10_serialize_boolean_union_value_request_json_unions() -> None:
    """Serializes a boolean union value"""
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        await client.json_unions(contents=MyUnionBooleanValue(value=True))
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert actual.destination.path == "/"
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("content-type", "application/x-amz-json-1.0"),
            ("x-amz-target", "JsonRpc10.JsonUnions"),
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = []
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = (
            b'{\n    "contents": {\n        "booleanValue": true\n    }\n}'
        )
        actual_body = json.loads(actual_body_content) if actual_body_content else ""
        expected_body = json.loads(expected_body_content)
        assert actual_body == expected_body

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


async def test_aws_json10_serialize_number_union_value_request_json_unions() -> None:
    """Serializes a number union value"""
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        await client.json_unions(contents=MyUnionNumberValue(value=1))
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert actual.destination.path == "/"
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("content-type", "application/x-amz-json-1.0"),
            ("x-amz-target", "JsonRpc10.JsonUnions"),
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = []
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = (
            b'{\n    "contents": {\n        "numberValue": 1\n    }\n}'
        )
        actual_body = json.loads(actual_body_content) if actual_body_content else ""
        expected_body = json.loads(expected_body_content)
        assert actual_body == expected_body

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


async def test_aws_json10_serialize_blob_union_value_request_json_unions() -> None:
    """Serializes a blob union value"""
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        await client.json_unions(contents=MyUnionBlobValue(value=b"foo"))
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert actual.destination.path == "/"
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("content-type", "application/x-amz-json-1.0"),
            ("x-amz-target", "JsonRpc10.JsonUnions"),
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = []
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = (
            b'{\n    "contents": {\n        "blobValue": "Zm9v"\n    }\n}'
        )
        actual_body = json.loads(actual_body_content) if actual_body_content else ""
        expected_body = json.loads(expected_body_content)
        assert actual_body == expected_body

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


async def test_aws_json10_serialize_timestamp_union_value_request_json_unions() -> None:
    """Serializes a timestamp union value"""
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        await client.json_unions(
            contents=MyUnionTimestampValue(
                value=datetime(2014, 4, 29, 18, 30, 38, 0, timezone.utc)
            )
        )
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert actual.destination.path == "/"
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("content-type", "application/x-amz-json-1.0"),
            ("x-amz-target", "JsonRpc10.JsonUnions"),
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = []
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = (
            b'{\n    "contents": {\n        "timestampValue": 1398796238\n    }\n}'
        )
        actual_body = json.loads(actual_body_content) if actual_body_content else ""
        expected_body = json.loads(expected_body_content)
        assert actual_body == expected_body

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


async def test_aws_json10_serialize_enum_union_value_request_json_unions() -> None:
    """Serializes an enum union value"""
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        await client.json_unions(contents=MyUnionEnumValue(value="Foo"))
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert actual.destination.path == "/"
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("content-type", "application/x-amz-json-1.0"),
            ("x-amz-target", "JsonRpc10.JsonUnions"),
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = []
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = (
            b'{\n    "contents": {\n        "enumValue": "Foo"\n    }\n}'
        )
        actual_body = json.loads(actual_body_content) if actual_body_content else ""
        expected_body = json.loads(expected_body_content)
        assert actual_body == expected_body

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


async def test_aws_json10_serialize_int_enum_union_value_request_json_unions() -> None:
    """Serializes an intEnum union value"""
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        await client.json_unions(contents=MyUnionIntEnumValue(value=1))
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert actual.destination.path == "/"
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("content-type", "application/x-amz-json-1.0"),
            ("x-amz-target", "JsonRpc10.JsonUnions"),
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = []
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = (
            b'{\n    "contents": {\n        "intEnumValue": 1\n    }\n}'
        )
        actual_body = json.loads(actual_body_content) if actual_body_content else ""
        expected_body = json.loads(expected_body_content)
        assert actual_body == expected_body

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


async def test_aws_json10_serialize_list_union_value_request_json_unions() -> None:
    """Serializes a list union value"""
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        await client.json_unions(contents=MyUnionListValue(value=["foo", "bar"]))
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert actual.destination.path == "/"
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("content-type", "application/x-amz-json-1.0"),
            ("x-amz-target", "JsonRpc10.JsonUnions"),
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = []
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = (
            b'{\n    "contents": {\n        "listValue": ["foo", "bar"]\n    }\n}'
        )
        actual_body = json.loads(actual_body_content) if actual_body_content else ""
        expected_body = json.loads(expected_body_content)
        assert actual_body == expected_body

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


async def test_aws_json10_serialize_map_union_value_request_json_unions() -> None:
    """Serializes a map union value"""
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        await client.json_unions(
            contents=MyUnionMapValue(value={"foo": "bar", "spam": "eggs"})
        )
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert actual.destination.path == "/"
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("content-type", "application/x-amz-json-1.0"),
            ("x-amz-target", "JsonRpc10.JsonUnions"),
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = []
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = b'{\n    "contents": {\n        "mapValue": {\n            "foo": "bar",\n            "spam": "eggs"\n        }\n    }\n}'
        actual_body = json.loads(actual_body_content) if actual_body_content else ""
        expected_body = json.loads(expected_body_content)
        assert actual_body == expected_body

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


async def test_aws_json10_serialize_structure_union_value_request_json_unions() -> None:
    """Serializes a structure union value"""
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        await client.json_unions(
            contents=MyUnionStructureValue(value=GreetingStruct(hi="hello"))
        )
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert actual.destination.path == "/"
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("content-type", "application/x-amz-json-1.0"),
            ("x-amz-target", "JsonRpc10.JsonUnions"),
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = []
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = b'{\n    "contents": {\n        "structureValue": {\n            "hi": "hello"\n        }\n    }\n}'
        actual_body = json.loads(actual_body_content) if actual_body_content else ""
        expected_body = json.loads(expected_body_content)
        assert actual_body == expected_body

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


async def test_aws_json10_deserialize_string_union_value_response_json_unions() -> None:
    """Deserializes a string union value"""
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[("Content-Type", "application/x-amz-json-1.0")],
                body=b'{\n    "contents": {\n        "stringValue": "foo"\n    }\n}',
            ),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        actual = await client.json_unions()
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = JsonUnionsOutput(contents=MyUnionStringValue(value="foo"))

        assert actual == expected


async def test_aws_json10_deserialize_boolean_union_value_response_json_unions() -> (
    None
):
    """Deserializes a boolean union value"""
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[("Content-Type", "application/x-amz-json-1.0")],
                body=b'{\n    "contents": {\n        "booleanValue": true\n    }\n}',
            ),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        actual = await client.json_unions()
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = JsonUnionsOutput(contents=MyUnionBooleanValue(value=True))

        assert actual == expected


async def test_aws_json10_deserialize_number_union_value_response_json_unions() -> None:
    """Deserializes a number union value"""
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[("Content-Type", "application/x-amz-json-1.0")],
                body=b'{\n    "contents": {\n        "numberValue": 1\n    }\n}',
            ),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        actual = await client.json_unions()
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = JsonUnionsOutput(contents=MyUnionNumberValue(value=1))

        assert actual == expected


async def test_aws_json10_deserialize_blob_union_value_response_json_unions() -> None:
    """Deserializes a blob union value"""
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[("Content-Type", "application/x-amz-json-1.0")],
                body=b'{\n    "contents": {\n        "blobValue": "Zm9v"\n    }\n}',
            ),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        actual = await client.json_unions()
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = JsonUnionsOutput(contents=MyUnionBlobValue(value=b"foo"))

        assert actual == expected


async def test_aws_json10_deserialize_timestamp_union_value_response_json_unions() -> (
    None
):
    """Deserializes a timestamp union value"""
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[("Content-Type", "application/x-amz-json-1.0")],
                body=b'{\n    "contents": {\n        "timestampValue": 1398796238\n    }\n}',
            ),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        actual = await client.json_unions()
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = JsonUnionsOutput(
            contents=MyUnionTimestampValue(
                value=datetime(2014, 4, 29, 18, 30, 38, 0, timezone.utc)
            )
        )

        assert actual == expected


async def test_aws_json10_deserialize_enum_union_value_response_json_unions() -> None:
    """Deserializes an enum union value"""
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[("Content-Type", "application/x-amz-json-1.0")],
                body=b'{\n    "contents": {\n        "enumValue": "Foo"\n    }\n}',
            ),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        actual = await client.json_unions()
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = JsonUnionsOutput(contents=MyUnionEnumValue(value="Foo"))

        assert actual == expected


async def test_aws_json10_deserialize_int_enum_union_value_response_json_unions() -> (
    None
):
    """Deserializes an intEnum union value"""
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[("Content-Type", "application/x-amz-json-1.0")],
                body=b'{\n    "contents": {\n        "intEnumValue": 1\n    }\n}',
            ),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        actual = await client.json_unions()
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = JsonUnionsOutput(contents=MyUnionIntEnumValue(value=1))

        assert actual == expected


async def test_aws_json10_deserialize_list_union_value_response_json_unions() -> None:
    """Deserializes a list union value"""
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[("Content-Type", "application/x-amz-json-1.0")],
                body=b'{\n    "contents": {\n        "listValue": ["foo", "bar"]\n    }\n}',
            ),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        actual = await client.json_unions()
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = JsonUnionsOutput(contents=MyUnionListValue(value=["foo", "bar"]))

        assert actual == expected


async def test_aws_json10_deserialize_map_union_value_response_json_unions() -> None:
    """Deserializes a map union value"""
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[("Content-Type", "application/x-amz-json-1.0")],
                body=b'{\n    "contents": {\n        "mapValue": {\n            "foo": "bar",\n            "spam": "eggs"\n        }\n    }\n}',
            ),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        actual = await client.json_unions()
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = JsonUnionsOutput(
            contents=MyUnionMapValue(value={"foo": "bar", "spam": "eggs"})
        )

        assert actual == expected


async def test_aws_json10_deserialize_structure_union_value_response_json_unions() -> (
    None
):
    """Deserializes a structure union value"""
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[("Content-Type", "application/x-amz-json-1.0")],
                body=b'{\n    "contents": {\n        "structureValue": {\n            "hi": "hello"\n        }\n    }\n}',
            ),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        actual = await client.json_unions()
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = JsonUnionsOutput(
            contents=MyUnionStructureValue(value=GreetingStruct(hi="hello"))
        )

        assert actual == expected


async def test_aws_json10_deserialize_ignore_type_response_json_unions() -> None:
    """Ignores an unrecognized __type property"""
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[("Content-Type", "application/x-amz-json-1.0")],
                body=b'{\n    "contents": {\n        "__type": "aws.protocoltests.json10#MyUnion",\n        "structureValue": {\n            "hi": "hello"\n        }\n    }\n}',
            ),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        actual = await client.json_unions()
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = JsonUnionsOutput(
            contents=MyUnionStructureValue(value=GreetingStruct(hi="hello"))
        )

        assert actual == expected


async def test_aws_json10_deserialize_allow_nulls_response_json_unions() -> None:
    """Allows for `: null` to be set for all unset fields"""
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[("Content-Type", "application/x-amz-json-1.0")],
                body=b'{\n    "contents": {\n      "stringValue": null,\n      "booleanValue": null,\n      "numberValue": null,\n      "blobValue": null,\n      "timestampValue": null,\n      "enumValue": null,\n      "intEnumValue": null,\n      "listValue": null,\n      "mapValue": null,\n      "structureValue": {\n          "hi": "hello"\n      }\n    }\n}',
            ),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        actual = await client.json_unions()
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = JsonUnionsOutput(
            contents=MyUnionStructureValue(value=GreetingStruct(hi="hello"))
        )

        assert actual == expected


async def test_aws_json10_must_always_send_empty_json_payload_request_no_input_and_no_output() -> (
    None
):
    """
    Clients must always send an empty JSON object payload for operations
    with no input (that is, `{}`). While AWS service implementations
    support requests with no payload or requests that send `{}`, always
    sending `{}` from the client is preferred for forward compatibility in
    case input is ever added to an operation.
    """
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        await client.no_input_and_no_output()
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert actual.destination.path == "/"
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("content-type", "application/x-amz-json-1.0"),
            ("x-amz-target", "JsonRpc10.NoInputAndNoOutput"),
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = []
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = b"{}"
        actual_body = json.loads(actual_body_content) if actual_body_content else ""
        expected_body = json.loads(expected_body_content)
        assert actual_body == expected_body

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


async def test_aws_json10_handles_empty_output_shape_response_no_input_and_no_output() -> (
    None
):
    """
    When no output is defined, the service is expected to return an empty
    payload, however, client must ignore a JSON payload if one is returned.
    This ensures that if output is added later, then it will not break the
    client.
    """
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[("Content-Type", "application/x-amz-json-1.0")],
                body=b"{}",
            ),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        actual = await client.no_input_and_no_output()
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = NoInputAndNoOutputOutput()

        assert actual == expected


async def test_aws_json10_handles_unexpected_json_output_response_no_input_and_no_output() -> (
    None
):
    """
    This client-only test builds on handles_empty_output_shape, by including
    unexpected fields in the JSON. A client needs to ignore JSON output that
    is empty or that contains JSON object data.
    """
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[("Content-Type", "application/x-amz-json-1.0")],
                body=b'{\n    "foo": true\n}',
            ),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        actual = await client.no_input_and_no_output()
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = NoInputAndNoOutputOutput()

        assert actual == expected


async def test_aws_json10_service_responds_with_no_payload_response_no_input_and_no_output() -> (
    None
):
    """
    When no output is defined, the service is expected to return an empty
    payload. Despite the lack of a payload, the service is expected to
    always send a Content-Type header. Clients must handle cases where a
    service returns a JSON object and where a service returns no JSON at
    all.
    """
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[("Content-Type", "application/x-amz-json-1.0")],
                body=b"",
            ),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        actual = await client.no_input_and_no_output()
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = NoInputAndNoOutputOutput()

        assert actual == expected


async def test_aws_json10_no_input_and_output_request_no_input_and_output() -> None:
    """A client should always send and empty JSON object payload."""
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        await client.no_input_and_output()
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert actual.destination.path == "/"
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("content-type", "application/x-amz-json-1.0"),
            ("x-amz-target", "JsonRpc10.NoInputAndOutput"),
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = []
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = b"{}"
        actual_body = json.loads(actual_body_content) if actual_body_content else ""
        expected_body = json.loads(expected_body_content)
        assert actual_body == expected_body

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


async def test_aws_json10_no_input_and_output_response_no_input_and_output() -> None:
    """Empty output always serializes an empty object payload."""
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[("Content-Type", "application/x-amz-json-1.0")],
                body=b"{}",
            ),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        actual = await client.no_input_and_output()
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = NoInputAndOutputOutput()

        assert actual == expected


@mark.xfail()
async def test_aws_json10_client_populates_default_values_in_input_request_operation_with_defaults() -> (
    None
):
    """Client populates default values in input."""
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        await client.operation_with_defaults(defaults=Defaults())
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert actual.destination.path == "/"
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("content-type", "application/x-amz-json-1.0")
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = []
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = b'{\n    "defaults": {\n        "defaultString": "hi",\n        "defaultBoolean": true,\n        "defaultList": [],\n        "defaultDocumentMap": {},\n        "defaultDocumentString": "hi",\n        "defaultDocumentBoolean": true,\n        "defaultDocumentList": [],\n        "defaultTimestamp": 0,\n        "defaultBlob": "YWJj",\n        "defaultByte": 1,\n        "defaultShort": 1,\n        "defaultInteger": 10,\n        "defaultLong": 100,\n        "defaultFloat": 1.0,\n        "defaultDouble": 1.0,\n        "defaultMap": {},\n        "defaultEnum": "FOO",\n        "defaultIntEnum": 1,\n        "emptyString": "",\n        "falseBoolean": false,\n        "emptyBlob": "",\n        "zeroByte": 0,\n        "zeroShort": 0,\n        "zeroInteger": 0,\n        "zeroLong": 0,\n        "zeroFloat": 0.0,\n        "zeroDouble": 0.0\n    }\n}'
        actual_body = json.loads(actual_body_content) if actual_body_content else ""
        expected_body = json.loads(expected_body_content)
        assert actual_body == expected_body

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


@mark.xfail()
async def test_aws_json10_client_skips_top_level_default_values_in_input_request_operation_with_defaults() -> (
    None
):
    """Client skips top level default values in input."""
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        await client.operation_with_defaults()
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert actual.destination.path == "/"
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("content-type", "application/x-amz-json-1.0")
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = []
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = b"{\n}"
        actual_body = json.loads(actual_body_content) if actual_body_content else ""
        expected_body = json.loads(expected_body_content)
        assert actual_body == expected_body

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


@mark.xfail()
async def test_aws_json10_client_uses_explicitly_provided_member_values_over_defaults_request_operation_with_defaults() -> (
    None
):
    """Client uses explicitly provided member values over defaults"""
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        await client.operation_with_defaults(
            defaults=Defaults(
                default_string="bye",
                default_boolean=True,
                default_list=["a"],
                default_document_map=Document({"name": "Jack"}),
                default_document_string=Document("bye"),
                default_document_boolean=Document(True),
                default_document_list=Document(["b"]),
                default_null_document=Document("notNull"),
                default_timestamp=datetime(1970, 1, 1, 0, 0, 1, 0, timezone.utc),
                default_blob=b"hi",
                default_byte=2,
                default_short=2,
                default_integer=20,
                default_long=200,
                default_float=float(2.0),
                default_double=float(2.0),
                default_map={"name": "Jack"},
                default_enum="BAR",
                default_int_enum=2,
                empty_string="foo",
                false_boolean=True,
                empty_blob=b"hi",
                zero_byte=1,
                zero_short=1,
                zero_integer=1,
                zero_long=1,
                zero_float=float(1.0),
                zero_double=float(1.0),
            )
        )
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert actual.destination.path == "/"
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("content-type", "application/x-amz-json-1.0")
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = []
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = b'{\n    "defaults": {\n        "defaultString": "bye",\n        "defaultBoolean": true,\n        "defaultList": ["a"],\n        "defaultDocumentMap": {"name": "Jack"},\n        "defaultDocumentString": "bye",\n        "defaultDocumentBoolean": true,\n        "defaultDocumentList": ["b"],\n        "defaultNullDocument": "notNull",\n        "defaultTimestamp": 1,\n        "defaultBlob": "aGk=",\n        "defaultByte": 2,\n        "defaultShort": 2,\n        "defaultInteger": 20,\n        "defaultLong": 200,\n        "defaultFloat": 2.0,\n        "defaultDouble": 2.0,\n        "defaultMap": {"name": "Jack"},\n        "defaultEnum": "BAR",\n        "defaultIntEnum": 2,\n        "emptyString": "foo",\n        "falseBoolean": true,\n        "emptyBlob": "aGk=",\n        "zeroByte": 1,\n        "zeroShort": 1,\n        "zeroInteger": 1,\n        "zeroLong": 1,\n        "zeroFloat": 1.0,\n        "zeroDouble": 1.0\n    }\n}'
        actual_body = json.loads(actual_body_content) if actual_body_content else ""
        expected_body = json.loads(expected_body_content)
        assert actual_body == expected_body

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


async def test_aws_json10_client_uses_explicitly_provided_values_in_top_level_request_operation_with_defaults() -> (
    None
):
    """
    Any time a value is provided for a member in the top level of input, it
    is used, regardless of if its the default.
    """
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        await client.operation_with_defaults(
            top_level_default="hi", other_top_level_default=0
        )
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert actual.destination.path == "/"
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("content-type", "application/x-amz-json-1.0")
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = []
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = (
            b'{\n    "topLevelDefault": "hi",\n    "otherTopLevelDefault": 0\n}'
        )
        actual_body = json.loads(actual_body_content) if actual_body_content else ""
        expected_body = json.loads(expected_body_content)
        assert actual_body == expected_body

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


@mark.xfail()
async def test_aws_json10_client_ignores_non_top_level_defaults_on_members_with_client_optional_request_operation_with_defaults() -> (
    None
):
    """
    Typically, non top-level members would have defaults filled in, but if
    they have the clientOptional trait, the defaults should be ignored.
    """
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        await client.operation_with_defaults(
            client_optional_defaults=ClientOptionalDefaults()
        )
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert actual.destination.path == "/"
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("content-type", "application/x-amz-json-1.0")
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = []
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = b'{\n    "clientOptionalDefaults": {}\n}'
        actual_body = json.loads(actual_body_content) if actual_body_content else ""
        expected_body = json.loads(expected_body_content)
        assert actual_body == expected_body

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


async def test_aws_json10_client_populates_defaults_values_when_missing_in_response_response_operation_with_defaults() -> (
    None
):
    """Client populates default values when missing in response."""
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[("Content-Type", "application/x-amz-json-1.0")],
                body=b"{}",
            ),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        actual = await client.operation_with_defaults()
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = OperationWithDefaultsOutput(
            default_string="hi",
            default_boolean=True,
            default_list=[],
            default_document_map=Document({}),
            default_document_string=Document("hi"),
            default_document_boolean=Document(True),
            default_document_list=Document([]),
            default_timestamp=datetime(1970, 1, 1, 0, 0, 0, 0, timezone.utc),
            default_blob=b"abc",
            default_byte=1,
            default_short=1,
            default_integer=10,
            default_long=100,
            default_float=float(1.0),
            default_double=float(1.0),
            default_map={},
            default_enum="FOO",
            default_int_enum=1,
            empty_string="",
            false_boolean=False,
            empty_blob=b"",
            zero_byte=0,
            zero_short=0,
            zero_integer=0,
            zero_long=0,
            zero_float=float(0.0),
            zero_double=float(0.0),
        )

        assert actual == expected


async def test_aws_json10_client_ignores_default_values_if_member_values_are_present_in_response_response_operation_with_defaults() -> (
    None
):
    """
    Client ignores default values if member values are present in the
    response.
    """
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[("Content-Type", "application/x-amz-json-1.0")],
                body=b'{\n    "defaultString": "bye",\n    "defaultBoolean": false,\n    "defaultList": ["a"],\n    "defaultDocumentMap": {"name": "Jack"},\n    "defaultDocumentString": "bye",\n    "defaultDocumentBoolean": false,\n    "defaultDocumentList": ["b"],\n    "defaultNullDocument": "notNull",\n    "defaultTimestamp": 2,\n    "defaultBlob": "aGk=",\n    "defaultByte": 2,\n    "defaultShort": 2,\n    "defaultInteger": 20,\n    "defaultLong": 200,\n    "defaultFloat": 2.0,\n    "defaultDouble": 2.0,\n    "defaultMap": {"name": "Jack"},\n    "defaultEnum": "BAR",\n    "defaultIntEnum": 2,\n    "emptyString": "foo",\n    "falseBoolean": true,\n    "emptyBlob": "aGk=",\n    "zeroByte": 1,\n    "zeroShort": 1,\n    "zeroInteger": 1,\n    "zeroLong": 1,\n    "zeroFloat": 1.0,\n    "zeroDouble": 1.0\n}',
            ),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        actual = await client.operation_with_defaults()
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = OperationWithDefaultsOutput(
            default_string="bye",
            default_boolean=False,
            default_list=["a"],
            default_document_map=Document({"name": "Jack"}),
            default_document_string=Document("bye"),
            default_document_boolean=Document(False),
            default_document_list=Document(["b"]),
            default_null_document=Document("notNull"),
            default_timestamp=datetime(1970, 1, 1, 0, 0, 2, 0, timezone.utc),
            default_blob=b"hi",
            default_byte=2,
            default_short=2,
            default_integer=20,
            default_long=200,
            default_float=float(2.0),
            default_double=float(2.0),
            default_map={"name": "Jack"},
            default_enum="BAR",
            default_int_enum=2,
            empty_string="foo",
            false_boolean=True,
            empty_blob=b"hi",
            zero_byte=1,
            zero_short=1,
            zero_integer=1,
            zero_long=1,
            zero_float=float(1.0),
            zero_double=float(1.0),
        )

        assert actual == expected


async def test_aws_json10_client_populates_nested_default_values_when_missing_request_operation_with_nested_structure() -> (
    None
):
    """Client populates nested default values when missing."""
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        await client.operation_with_nested_structure(
            top_level=TopLevel(
                dialog=Dialog(language="en"),
                dialog_list=[
                    Dialog(),
                    Dialog(farewell=Farewell()),
                    Dialog(
                        language="it",
                        greeting="ciao",
                        farewell=Farewell(phrase="arrivederci"),
                    ),
                ],
                dialog_map={
                    "emptyDialog": Dialog(),
                    "partialEmptyDialog": Dialog(language="en", farewell=Farewell()),
                    "nonEmptyDialog": Dialog(
                        greeting="konnichiwa", farewell=Farewell(phrase="sayonara")
                    ),
                },
            )
        )
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert actual.destination.path == "/"
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("content-type", "application/x-amz-json-1.0")
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = []
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = b'{\n    "topLevel": {\n        "dialog": {\n            "language": "en",\n            "greeting": "hi"\n        },\n        "dialogList": [\n            {\n                "greeting": "hi"\n            },\n            {\n                "greeting": "hi",\n                "farewell": {\n                    "phrase": "bye"\n                }\n            },\n            {\n                "language": "it",\n                "greeting": "ciao",\n                "farewell": {\n                    "phrase": "arrivederci"\n                }\n            }\n        ],\n        "dialogMap": {\n            "emptyDialog": {\n                "greeting": "hi"\n            },\n            "partialEmptyDialog": {\n                "language": "en",\n                "greeting": "hi",\n                "farewell": {\n                    "phrase": "bye"\n                }\n            },\n            "nonEmptyDialog": {\n                "greeting": "konnichiwa",\n                "farewell": {\n                    "phrase": "sayonara"\n                }\n            }\n        }\n    }\n}'
        actual_body = json.loads(actual_body_content) if actual_body_content else ""
        expected_body = json.loads(expected_body_content)
        assert actual_body == expected_body

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


async def test_aws_json10_client_populates_nested_defaults_when_missing_in_response_body_response_operation_with_nested_structure() -> (
    None
):
    """Client populates nested default values when missing in response body."""
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[("Content-Type", "application/x-amz-json-1.0")],
                body=b'{\n    "dialog": {\n        "language": "en"\n    },\n    "dialogList": [\n        {\n        },\n        {\n            "farewell": {}\n        },\n        {\n            "language": "it",\n            "greeting": "ciao",\n            "farewell": {\n                "phrase": "arrivederci"\n            }\n        }\n    ],\n    "dialogMap": {\n        "emptyDialog": {\n        },\n        "partialEmptyDialog": {\n            "language": "en",\n            "farewell": {}\n        },\n        "nonEmptyDialog": {\n            "greeting": "konnichiwa",\n            "farewell": {\n                "phrase": "sayonara"\n            }\n        }\n    }\n}',
            ),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        actual = await client.operation_with_nested_structure()
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = OperationWithNestedStructureOutput(
            dialog=Dialog(language="en", greeting="hi"),
            dialog_list=[
                Dialog(greeting="hi"),
                Dialog(greeting="hi", farewell=Farewell(phrase="bye")),
                Dialog(
                    language="it",
                    greeting="ciao",
                    farewell=Farewell(phrase="arrivederci"),
                ),
            ],
            dialog_map={
                "emptyDialog": Dialog(greeting="hi"),
                "partialEmptyDialog": Dialog(
                    language="en", greeting="hi", farewell=Farewell(phrase="bye")
                ),
                "nonEmptyDialog": Dialog(
                    greeting="konnichiwa", farewell=Farewell(phrase="sayonara")
                ),
            },
        )

        assert actual == expected


async def test_aws_json10_client_error_corrects_when_server_fails_to_serialize_required_values_response_operation_with_required_members() -> (
    None
):
    """Client error corrects when server fails to serialize required values."""
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[("Content-Type", "application/x-amz-json-1.0")],
                body=b"{}",
            ),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        actual = await client.operation_with_required_members()
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = OperationWithRequiredMembersOutput(
            required_string="",
            required_boolean=False,
            required_list=[],
            required_timestamp=datetime(1970, 1, 1, 0, 0, 0, 0, timezone.utc),
            required_blob=b"",
            required_byte=0,
            required_short=0,
            required_integer=0,
            required_long=0,
            required_float=float(0.0),
            required_double=float(0.0),
            required_map={},
        )

        assert actual == expected


async def test_aws_json10_client_error_corrects_with_default_values_when_server_fails_to_serialize_required_values_response_operation_with_required_members_with_defaults() -> (
    None
):
    """
    Client error corrects with default values when server fails to serialize
    required values.
    """
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[("Content-Type", "application/x-amz-json-1.0")],
                body=b"{}",
            ),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        actual = await client.operation_with_required_members_with_defaults()
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = OperationWithRequiredMembersWithDefaultsOutput(
            required_string="hi",
            required_boolean=True,
            required_list=[],
            required_timestamp=datetime(1970, 1, 1, 0, 0, 1, 0, timezone.utc),
            required_blob=b"blob",
            required_byte=1,
            required_short=1,
            required_integer=10,
            required_long=100,
            required_float=float(1.0),
            required_double=float(1.0),
            required_map={},
            required_enum="FOO",
            required_int_enum=1,
        )

        assert actual == expected


@mark.xfail()
async def test_sdk_applied_content_encoding_aws_json1_0_request_put_with_content_encoding() -> (
    None
):
    """
    Compression algorithm encoding is appended to the Content-Encoding
    header.
    """
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        await client.put_with_content_encoding(
            data="\n"
        )
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert actual.destination.path == "/"
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [("content-encoding", "gzip")]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = []
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


@mark.xfail()
async def test_sdk_appends_gzip_and_ignores_http_provided_encoding_aws_json1_0_request_put_with_content_encoding() -> (
    None
):
    """
    Compression algorithm encoding is appended to the Content-Encoding
    header, and the user-provided content-encoding is NOT in the
    Content-Encoding header since HTTP binding traits are ignored in the
    awsJson1_0 protocol.
    """
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        await client.put_with_content_encoding(
            encoding="custom",
            data="\n",
        )
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert actual.destination.path == "/"
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [("content-encoding", "gzip")]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = []
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


async def test_non_query_compatible_aws_json10_forbids_query_mode_header_request_query_incompatible_operation() -> (
    None
):
    """The query mode header MUST NOT be set on non-query-compatible services."""
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        await client.query_incompatible_operation()
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert actual.destination.path == "/"
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("content-type", "application/x-amz-json-1.0"),
            ("x-amz-target", "JsonRpc10.QueryIncompatibleOperation"),
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = []
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = b"{}"
        actual_body = json.loads(actual_body_content) if actual_body_content else ""
        expected_body = json.loads(expected_body_content)
        assert actual_body == expected_body

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


@mark.xfail()
async def test_aws_json10_supports_na_n_float_inputs_request_simple_scalar_properties() -> (
    None
):
    """Supports handling NaN float values."""
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        await client.simple_scalar_properties(
            float_value=float("nan"), double_value=float("nan")
        )
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert actual.destination.path == "/"
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("content-type", "application/x-amz-json-1.0"),
            ("x-amz-target", "JsonRpc10.SimpleScalarProperties"),
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = []
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = (
            b'{\n    "floatValue": "NaN",\n    "doubleValue": "NaN"\n}'
        )
        actual_body = json.loads(actual_body_content) if actual_body_content else ""
        expected_body = json.loads(expected_body_content)
        assert actual_body == expected_body

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


async def test_aws_json10_supports_infinity_float_inputs_request_simple_scalar_properties() -> (
    None
):
    """Supports handling Infinity float values."""
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        await client.simple_scalar_properties(
            float_value=float("inf"), double_value=float("inf")
        )
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert actual.destination.path == "/"
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("content-type", "application/x-amz-json-1.0"),
            ("x-amz-target", "JsonRpc10.SimpleScalarProperties"),
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = []
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = (
            b'{\n    "floatValue": "Infinity",\n    "doubleValue": "Infinity"\n}'
        )
        actual_body = json.loads(actual_body_content) if actual_body_content else ""
        expected_body = json.loads(expected_body_content)
        assert actual_body == expected_body

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


async def test_aws_json10_supports_negative_infinity_float_inputs_request_simple_scalar_properties() -> (
    None
):
    """Supports handling -Infinity float values."""
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com/",
            transport=RequestTestHTTPClient(),
            retry_strategy=SimpleRetryStrategy(max_attempts=1),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        await client.simple_scalar_properties(
            float_value=float("-inf"), double_value=float("-inf")
        )
        fail("Expected 'TestHttpServiceError' exception to be thrown!")
    except TestHttpServiceError as err:
        actual = err.request

        assert actual.method == "POST"
        assert actual.destination.path == "/"
        assert actual.destination.host == "example.com"

        query = actual.destination.query
        actual_query_segments: list[str] = query.split("&") if query else []
        expected_query_segments: list[str] = []
        for expected_query_segment in expected_query_segments:
            assert expected_query_segment in actual_query_segments
            actual_query_segments.remove(expected_query_segment)

        actual_query_keys: list[str] = [k.lower() for k, v in parse_qsl(query)]
        forbidden_query_keys: set[str] = set([])
        for forbidden_key in forbidden_query_keys:
            assert forbidden_key.lower() not in actual_query_keys

        required_query_keys: list[str] = []
        for required_query_key in required_query_keys:
            assert required_query_key.lower() in actual_query_keys
            # These are removed because the required list could require more than one
            # value. By removing each value after we assert that it's there, we can
            # effectively validate that without having to have a more complex comparator.
            actual_query_keys.remove(required_query_key)

        expected_headers: list[tuple[str, str]] = [
            ("content-type", "application/x-amz-json-1.0"),
            ("x-amz-target", "JsonRpc10.SimpleScalarProperties"),
        ]
        for expected_key, expected_val in expected_headers:
            assert expected_val in actual.fields[expected_key].values

        forbidden_headers: set[str] = set([])
        for forbidden_key in forbidden_headers:
            with raises(KeyError):
                actual.fields[forbidden_key]

        required_headers: list[str] = []
        for required_key in required_headers:
            # del Fields[required_key] raises KeyError if key does not exist
            del actual.fields[required_key]

        actual_body_content = await AsyncBytesReader(actual.body or b"").read()
        expected_body_content = (
            b'{\n    "floatValue": "-Infinity",\n    "doubleValue": "-Infinity"\n}'
        )
        actual_body = json.loads(actual_body_content) if actual_body_content else ""
        expected_body = json.loads(expected_body_content)
        assert actual_body == expected_body

    except Exception as err:
        fail(
            f"Expected 'TestHttpServiceError' exception to be thrown, but received {type(err).__name__}: {err}"
        )


@mark.xfail()
async def test_aws_json10_supports_na_n_float_inputs_response_simple_scalar_properties() -> (
    None
):
    """Supports handling NaN float values."""
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[("Content-Type", "application/x-amz-json-1.0")],
                body=b'{\n    "floatValue": "NaN",\n    "doubleValue": "NaN"\n}',
            ),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        actual = await client.simple_scalar_properties()
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = SimpleScalarPropertiesOutput(
            float_value=float("nan"), double_value=float("nan")
        )

        assert actual == expected


async def test_aws_json10_supports_infinity_float_inputs_response_simple_scalar_properties() -> (
    None
):
    """Supports handling Infinity float values."""
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[("Content-Type", "application/x-amz-json-1.0")],
                body=b'{\n    "floatValue": "Infinity",\n    "doubleValue": "Infinity"\n}',
            ),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        actual = await client.simple_scalar_properties()
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = SimpleScalarPropertiesOutput(
            float_value=float("inf"), double_value=float("inf")
        )

        assert actual == expected


async def test_aws_json10_supports_negative_infinity_float_inputs_response_simple_scalar_properties() -> (
    None
):
    """Supports handling -Infinity float values."""
    client = AsyncJSONRPC10Client(
        config=await AsyncJSONRPC10Config.resolve(
            endpoint_uri="https://example.com",
            transport=ResponseTestHTTPClient(
                status=200,
                headers=[("Content-Type", "application/x-amz-json-1.0")],
                body=b'{\n    "floatValue": "-Infinity",\n    "doubleValue": "-Infinity"\n}',
            ),
            region="us-east-1",
            aws_access_key_id="test-access-key-id",
            aws_secret_access_key="test-secret-access-key",
            aws_credentials_identity_resolver=StaticCredentialsResolver(),
        )
    )

    try:
        actual = await client.simple_scalar_properties()
    except Exception as err:
        fail(f"Expected a valid response, but received: {type(err).__name__}: {err}")
    else:
        expected = SimpleScalarPropertiesOutput(
            float_value=float("-inf"), double_value=float("-inf")
        )

        assert actual == expected


class TestHttpServiceError(ServiceError):
    """A test error that subclasses the service-error for protocol tests."""

    def __init__(self, request: HTTPRequest):
        self.request = request


class RequestTestHTTPClient:
    """An asynchronous HTTP client solely for testing purposes."""

    TIMEOUT_EXCEPTIONS = ()
    SUPPORTS_DUPLEX_STREAMING: bool = True

    def __init__(self, *, client_config: HTTPClientConfiguration | None = None):
        self._client_config = client_config

    async def send(
        self,
        request: HTTPRequest,
        *,
        request_config: HTTPRequestConfiguration | None = None,
    ) -> _smithy_http_aio_interfaces_HTTPResponse:
        # Raise the exception with the request object to bypass actual request handling
        raise TestHttpServiceError(request)


class ResponseTestHTTPClient:
    """An asynchronous HTTP client solely for testing purposes."""

    TIMEOUT_EXCEPTIONS = ()
    SUPPORTS_DUPLEX_STREAMING: bool = True

    def __init__(
        self,
        *,
        client_config: HTTPClientConfiguration | None = None,
        status: int = 200,
        headers: list[tuple[str, str]] | None = None,
        body: bytes = b"",
    ):
        self._client_config = client_config
        self.status = status
        self.fields = tuples_to_fields(headers or [])
        self.body = body

    async def send(
        self,
        request: HTTPRequest,
        *,
        request_config: HTTPRequestConfiguration | None = None,
    ) -> _smithy_http_aio_HTTPResponse:
        # Pre-construct the response from the request and return it
        return _smithy_http_aio_HTTPResponse(
            status=self.status, fields=self.fields, body=async_list([self.body])
        )
